# ADR Index

Реестр архитектурных решений (Architecture Decision Records). Формат файла: `ADR-NNNN-<slug>.md`. Каждый ADR содержит секции **Context / Decision / Consequences / Alternatives considered**. Решения иммутабельны: если решение меняется — заводится новый ADR со ссылкой на устаревший, статус старого меняется на `superseded by ADR-XXXX`.

**Open-questions нумерация.** ADR-локальные вопросы используют схему `Q-<ADR>-<seq>` (например, `Q-0029-1`, `Q-WH-*`, `Q-OAUTH-*`) — это **намеренно** локальная нумерация в пределах ADR и **не** попадает в сквозной реестр `99-open-questions.md` (`Q-NNN-N`). Глобальная `Q-NNN` — для cross-cutting вопросов проекта; `Q-<ADR>-<seq>` — для вопросов, закрываемых внутри своего ADR.

| ID | Название | Статус | Дата |
| --- | --- | --- | --- |
| [ADR-0001](./ADR-0001-tech-stack.md) | Базовый технологический стек (FastAPI + PostgreSQL + MinIO + Jinja2) | accepted | 2026-05-05 |
| [ADR-0002](./ADR-0002-imap-library.md) | Выбор IMAP/SMTP-библиотек (imap-tools sync в worker, aiosmtplib async) | accepted | 2026-05-05 |
| [ADR-0003](./ADR-0003-worker.md) | Background worker — APScheduler в отдельном контейнере | accepted | 2026-05-05 |
| [ADR-0004](./ADR-0004-sessions.md) | Сессии — server-side через Redis, opaque cookie | accepted | 2026-05-05 |
| [ADR-0005](./ADR-0005-encryption.md) | Шифрование почтовых паролей — AES-256-GCM с per-record IV | accepted | 2026-05-05 |
| [ADR-0006](./ADR-0006-password-hashing.md) | Хеширование паролей пользователей — argon2id | accepted | 2026-05-05 |
| [ADR-0007](./ADR-0007-storage-scheme.md) | Схема хранения вложений в MinIO | accepted | 2026-05-05 |
| [ADR-0008](./ADR-0008-sync-strategy.md) | Стратегия инкрементальной IMAP-синхронизации (UIDNEXT-based, 30-day initial backfill) | accepted | 2026-05-05 |
| [ADR-0009](./ADR-0009-rate-limiting.md) | Rate-limit на login и password-set | accepted | 2026-05-05 |
| [ADR-0010](./ADR-0010-csrf-protection.md) | CSRF-защита для всех cookie-аутентифицированных POST | accepted | 2026-05-05 |
| [ADR-0011](./ADR-0011-retention.md) | Ретенция писем и вложений — 30 дней, daily cleanup | accepted | 2026-05-05 |
| [ADR-0012](./ADR-0012-message-body-storage.md) | Хранение тел писем — plain text only в PostgreSQL (TEXT) | accepted | 2026-05-05 |
| [ADR-0013](./ADR-0013-concurrency-model.md) | Конкурентность IMAP-сессий — asyncio.Semaphore=10 + thread pool для sync-библиотеки | accepted | 2026-05-05 |
| [ADR-0014](./ADR-0014-logging.md) | Логирование — structlog, JSON в stdout, request_id correlation | accepted | 2026-05-05 |
| [ADR-0015](./ADR-0015-no-js-fallback.md) | No-JS fallback — `_method` override + form-encoded acceptance + content negotiation | accepted | 2026-05-05 |
| [ADR-0016](./ADR-0016-two-step-login.md) | Two-step login (username then password, ADR-style flow as Google) | accepted | 2026-05-06 |
| [ADR-0017](./ADR-0017-tags.md) | Теги для писем — rule-based авто-классификация и пользовательские правила | accepted | 2026-05-07 |
| [ADR-0018](./ADR-0018-telegram-launcher.md) | Telegram launcher bot + WebApp без линковки аккаунтов | partially superseded by ADR-0022 | 2026-05-07 |
| [ADR-0019](./ADR-0019-groups-and-roles.md) | Роли пользователей и группы (super_admin / group_leader / group_member) с visibility-моделью | accepted (§11/A2 «отказ от multi-group» superseded by ADR-0030) | 2026-05-08 |
| [ADR-0020](./ADR-0020-mail-account-nickname.md) | Никнейм (display_name) у mail-аккаунтов | accepted | 2026-05-08 |
| [ADR-0021](./ADR-0021-russian-localization.md) | Полная RU-локализация UI без i18n-фреймворка | accepted | 2026-05-08 |
| [ADR-0022](./ADR-0022-telegram-sso-and-notifications.md) | Telegram persistent SSO (initData HMAC + `telegram_links`) + push-уведомления о письмах с тегами (отменяет «без линковки» из ADR-0018, закрывает TD-013) | accepted | 2026-05-13 |
| [ADR-0023](./ADR-0023-outbound-webhooks.md) | Outbound webhooks для команд (один webhook на `group_id`, AES-GCM secret, dispatcher по паттерну ADR-0022, фильтр «не флудим историей») | accepted | 2026-05-20 |
| [ADR-0024](./ADR-0024-multi-telegram-links.md) | Несколько Telegram-привязок на один аккаунт (снятие `UNIQUE(telegram_links.user_id)`, ключ идемпотентности `telegram_notifications` → `(message_id, telegram_user_id)`, доставка во все живые чаты; расширяет ADR-0022) | accepted | 2026-05-27 |
| [ADR-0025](./ADR-0025-outlook-oauth2.md) | OAuth2 (XOAUTH2) для личных Outlook (consent через сайт+OctoBrowser, IMAP/SMTP XOAUTH2 напрямую, refresh-токен AES-GCM, расширение `mail_accounts`) | accepted | 2026-05-27 |
| [ADR-0026](./ADR-0026-sync-error-resilience.md) | Отказоустойчивость синхронизации: классификация transient/permanent (единая таблица + приоритеты, «too many connections» = transient), transient не дисейблит, circuit-breaker против массового disable, DNS/connect-retry, само-восстановление через `mark_sync_success`; extends ADR-0008. **Амендирован [ADR-0046](./ADR-0046-mailbox-status-hook-points.md)** (реализовано, `e7c7b52`): запись `last_synced_at=now()` в PERMANENT-ветке **отменена** (поле = «последний УСПЕШНЫЙ синк» без исключений; единственный писатель — `mark_sync_success`) | accepted (амендирован ADR-0046) | 2026-05-28 |
| [ADR-0027](./ADR-0027-push-team-bots.md) | Push-only Telegram-боты по командам (round-31: `ivan`/`alexandra`/`andrei`; **round-44: + `business2` → 4 бота**): отдельная очередь `push_notify_queue` + worker-job `push_notify_dispatch`, маппинг бот→команда явным `group_id` в `.env`, фиксированные `ADMIN_TELEGRAM_IDS`, fire-and-forget без БД-трекинга/recovery; reuse `format_notification`/`send_notification` (токен-параметризация); extends ADR-0022 §2, основной бот не тронут. **round-42:** push-боты получают callback-кнопку «Посмотреть сообщение» + собственный webhook (`/api/telegram/push-webhook/{bot_name}`, per-бот `BOT_*_WEBHOOK_SECRET`); callback авторизуется по `ADMIN_TELEGRAM_IDS` + group-match (§10–§11); токены/secret теперь нужны и в api. **round-44:** 4-й бот `business2` (`BOT_BUSINESS2_TOKEN`/`_GROUP_ID`/`_WEBHOOK_SECRET`, прод `group_id` задаёт оператор в `.env`, ≠1/2/3); роут и диспатчер generic — change только config+redact, новых endpoint/job/миграций нет | accepted | 2026-06-09 (upd round-42; round-44 +business2) |
| [ADR-0028](./ADR-0028-oauth-login-failed-transient.md) | OAuth `Login failed` от Microsoft IMAP = **transient** (контекстная классификация по `auth_type`): refresh успешен ДО IMAP ⇒ `login failed`/`authenticationfailed` у `oauth_outlook` — серверная флуктуация, не permanent (retry + no-disable + suppress); password-поведение неизменно; без миграции, kill-switch `SYNC_OAUTH_LOGIN_FAILED_TRANSIENT`; extends ADR-0026 §1/§2/§4 | accepted | 2026-06-10 |
| [ADR-0029](./ADR-0029-external-pull-api.md) | External PULL-API для B2B-партнёра: `GET /api/external/messages?since_id&limit` — инкрементальный keyset по `messages.id BIGSERIAL` (id ASC, без пропусков/дублей; retention даёт безвредные id-gaps), ВСЕ письма системы (super_admin visibility, без сессии), СЫРОЕ полное тело (`body_text`+`body_html`, без collapse-нормализации), `to_addrs`/`cc_addrs` included, вложения нет; auth static `EXTERNAL_API_KEY` (`compare_digest`, `X-API-Key`/`Bearer`), отдельный версионируемый `ExternalMessageDTO`; без миграции БД | accepted | 2026-06-11 |
| [ADR-0030](./ADR-0030-multi-group-membership.md) | Членство пользователя в нескольких командах (multi-group): аддитивная M:N-таблица `user_groups` — источник истины для видимости/уведомлений; `users.group_id` сохраняется как «домашняя» команда (инвариант лидера, присвоение `mail_accounts.group_id`, роль); видимость консистентна с уведомлениями (член N команд видит ящики/письма и получает TG-уведомления всех своих команд); `mail_accounts.group_id` 1:1 не меняется; добавлять можно всех кроме super_admin; перенос лидера запрещён; endpoints `POST`/`DELETE /api/admin/users/{id}/groups`; push-боты ADR-0027 не тронуты (broadcast по `account.group_id`, не по членству); **supersedes ADR-0019 §11/A2 (partial)** | accepted | 2026-06-23 |
| [ADR-0031](./ADR-0031-mailbox-team-selection.md) | Выбор и смена команды для почтового ящика: опциональный `group_id` в create (`MailAccountCreateRequest`) — без него прежнее поведение (домашняя группа владельца / NULL для super_admin self); перенос существующего ящика через расширение `PATCH /api/mail-accounts/{id}` полем `group_id` (БЕЗ нового form-fallback regex — счётчик `_OVERRIDE_REGEX_PATHS=16` не меняется); единые правила допустимости целевой команды (member — свои `user_groups`/только себе; leader — свои команды себе или своя команда участнику; super_admin — любая/NULL; нарушение → 400/403/404, не 500); transfer запрещён для `group_member`; источник списка — новый `GET /api/my/groups` (reuse `GroupsRepo`); audit `mail_account_group_change`; миграция БД не нужна (`mail_accounts.group_id` уже есть); **extends ADR-0019 §8 / ADR-0030** | accepted | 2026-06-23 |
| [ADR-0032](./ADR-0032-dedicated-server-and-host-certbot.md) | Выделенный prod-сервер Hetzner (`49.12.189.77`, Ubuntu 26.04, домен `postapp.store`) взамен общего `132.243.113.117` + закрепление host-level TLS: `certbot` пакет + `certbot.timer` (webroot `/var/www/certbot`), host `/etc/letsencrypt` bind-mount RO в контейнер nginx — **нет** сервиса certbot и volume `mas_certbot_certs` в compose (устранение docs-дрейфа §2/§6); фактические GitHub Secrets `DEPLOY_HOST`/`DEPLOY_USER=root`/`DEPLOY_KEY_B64`(base64)/`DEPLOY_PATH=/opt/mail-agregator`; GHCR-образы публичные (без `docker login`); runbook переезда — `07-deployment.md` sec. 15 | accepted | 2026-07-01 |
| [ADR-0033](./ADR-0033-mailbox-down-telegram-alert.md) | Telegram-оповещение о нерабочей (авто-отключённой) почте: триггер в единственной точке auto-disable `worker.sync_cycle._disable_after_failures` (ADR-0026 §3; circuit-breaker/transient/needs-consent/ручное отключение НЕ триггерят); идемпотентность «ровно один алерт на переход» через новый столбец `mail_accounts.disabled_alert_sent_at TIMESTAMPTZ NULL` (guarded set `WHERE ... IS NULL` в disable-транзакции; reset в NULL на re-enable); получатели — новый `TelegramNotificationsRepo.list_recipients_for_mailbox(id)` с тем же предикатом видимости (super_admin/членство `user_groups` ADR-0030/owner + живой `telegram_links` + `tg_notifications_enabled`), но БЕЗ per-message-предикатов (`internal_date`/tag); доставка **основным** ботом ADR-0022 через новую очередь `mailbox_alert_queue` + APScheduler-job `mailbox_alert_dispatch`, per-chat dedup, fire-and-forget (TD-042); текст «⚠️ Почта … не работает: …»; миграция `20260703_020`; **extends ADR-0022 §2 / ADR-0026 / ADR-0030** | accepted | 2026-07-03 |
| [ADR-0034](./ADR-0034-leader-mail-forwarding.md) | Переадресация входящих писем команды на почту лидера: per-group конфиг **одна запись на команду** `group_forwarding` (`group_id UNIQUE`, `forward_to`, `is_active`) + дедуп-claim `message_forwards` (`UNIQUE(message_id, group_id)`, claim `INSERT … ON CONFLICT DO NOTHING RETURNING` ДО отправки → exactly-once); миграция `20260703_021`; CRUD `GET/PUT(upsert)/DELETE /api/forwarding/me` + form-fallback (2 exact-пути → `_OVERRIDE_EXACT_PATHS` 5→7, регекс-16 без изменений); пайплайн-форк webhooks — хук в `sync_cycle` (enqueue **всех** новых входящих ящиков команды `group_id IS NOT NULL`, loop-guard на enqueue-side: skip уже несущих `X-Forwarded-By: mail-aggregator`) → `forward_dispatch_queue` → job `forward_dispatch` (temporal-guard `internal_date >= gf.created_at`, loop-guard `forward_to != account.email`, claim, `build_forward_mime` multipart text+html+вложения из MinIO с `FORWARD_MAX_TOTAL_BYTES`-лимитом, oversized пропуск с пометкой) → SMTP **кредами получившего ящика** (`From`=ящик, `To`=лидер, блок «пересланное сообщение»; reuse `smtp_send_message` password/XOAUTH2; **без** Sent-append); только команда (персональные `group_id NULL` не шлём), только новые после включения; fire-and-forget без recovery/retry (TD-043); фиче-флаг `FORWARDING_ENABLED`; audit `forwarding_updated`/`forwarding_deleted`; **форкает ADR-0023, extends ADR-0030 / ADR-0015 / ADR-0025** | accepted | 2026-07-03 |
| [ADR-0035](./ADR-0035-external-reply-endpoint.md) | External reply-endpoint — единственный scoped write во внешнем API: `POST /api/external/messages/{id}/reply` (ответ на существующее письмо тем же `X-API-Key`). Не рушит read-only ADR-0029 — узкая поверхность: только ответ на видимое в pull письмо, отправитель **не выбирается** (`from`=`mail_account` оригинала), нет CRUD/произвольной отправки/`from_account_id`; threading `In-Reply-To`/`References` по `{id}`. Write **opt-in** отдельным флагом `EXTERNAL_REPLY_ENABLED` (default `false`) — существующие read-only-деплои write не получают молча. Отдельный rate-limit `LIMIT_EXTERNAL_REPLY` (`EXTERNAL_REPLY_RATE_LIMIT_PER_MINUTE`, default 30/min per IP, < read 120/min — write дороже/abuse-опаснее). Тело `ExternalReplyRequest{to?,cc?,subject?,body}` (`to` default `[оригинал.from_addr]`, `subject` default `"Re: "+оригинал.subject`, `body` обяз. непустой ≤1 MiB); ответ `ExternalReplyResponse{sent_id,smtp_message_id}` (без `appended_to_sent`); коды 200/400/401/403/404/409/429/502. Backend: новый `SendService.send_external_reply` (canonical-scope резолв → 404, `from`=`original.mail_account_id`, author `user_id`=владелец ящика) поверх общего send-ядра (MIME/SMTP/append **не** дублируются); без миграции БД; **extends ADR-0029, reuse ADR-0034 §5 SMTP-ядра** | accepted | 2026-07-03 |
| [ADR-0036](./ADR-0036-external-backward-pagination.md) | Backward / newest-first пагинация внешнего PULL-API: тот же `GET /api/external/messages` + явный `order` (enum `asc`\|`desc`, default `asc`) и обратный курсор `before_id` (`int`, `ge=1`, только в `desc`). `order=desc` без `before_id` → самые свежие N (`ORDER BY id DESC LIMIT`), с `before_id` → страница `id < before_id` DESC; ответ `desc` — `{messages(id DESC), next_before_id:int\|null (=min(id) или null), has_more}`. Forward ADR-0029 (`asc`/`since_id`/`next_since_id`) **байт-в-байт неизменен** (BC). Режимы взаимоисключающи: `before_id`@`asc`, `since_id`@`desc`, оба курсора вместе → `400 validation_error`. Курсор по монотонному **`id`** (не `internal_date`) — снимает ограничение ADR-0029 «нет newest-first» без silent-loss поздних писем. Auth/rate-limit `LIMIT_EXTERNAL_API`/CSRF-exempt/redact/super_admin-visibility/canonical-дедуп/`ExternalMessageDTO`+`ExternalTagDTO` — те же; **без миграции БД, без новых индексов (reverse-scan по PK `id`), без новых env/флагов**; reply ADR-0035 и read-only ADR-0029 не затронуты; **extends ADR-0029** | accepted | 2026-07-04 |
| [ADR-0037](./ADR-0037-external-teams-mailboxes-message-filters.md) | External API для CRM: два read-only endpoint'а `GET /api/external/teams` (`{teams:[{id,name}]}`, source `GroupsRepo.list_all_groups()`) и `GET /api/external/mailboxes` (`{mailboxes:[{id,email,display_name,group_id,is_active}]}`, canonical-дедуп `list_by_ids(list_canonical_account_ids())`) + два **опциональных** query-фильтра к `GET /api/external/messages`: `mail_account_id` (`ge=1`) и `group_id` (`ge=1`), работают в обоих режимах asc/desc (ADR-0036), сужают набор `mail_account_ids` (пересечение с canonical); взаимоисключение `mail_account_id`×`group_id` → `400 validation_error` `field=filter`; несуществующий/чужой/non-canonical id → **пустая страница** (не 404, инвариант ADR-0029 §3); message-DTO (`ExternalMessageDTO`/`ExternalMailAccountDTO`/`ExternalTagDTO`) **не меняется** (`group_id`/`is_active` — только в новом `mailboxes`, не в письме); тот же `EXTERNAL_API_KEY`/`LIMIT_EXTERNAL_API`/CSRF-exempt/super_admin-visibility/redact; `group_id`/`is_active` раскрыты осознанно для внутреннего CRM; без миграции БД, без новых env/флагов/индексов; **extends ADR-0029, reuse canonical-дедуп §5 + оба режима ADR-0036** | accepted (взаимоисключение `mail_account_id`×`group_id` **superseded by ADR-0039** §3 — фильтры стали AND-комбинируемыми) | 2026-07-06 |
| [ADR-0039](./ADR-0039-external-write-api.md) | External write API (headless CRM): раздел `/api/external/mailboxes` write — `POST /mailboxes/test`, `POST /mailboxes`, `PATCH/DELETE /mailboxes/{id}`, `POST /mailboxes/{id}/sync` (reuse `accounts/service.py` + SSRF-guard); раздел `/api/external/tags` CRUD (ADR-0040); гейт **`EXTERNAL_WRITE_ENABLED`** (default `false`, по образцу `EXTERNAL_REPLY_ENABLED`) + отдельный budget **`LIMIT_EXTERNAL_WRITE`** (`EXTERNAL_WRITE_RATE_LIMIT_PER_MINUTE` default 60/min per IP), auth-flow ADR-0029 §4/ADR-0035 §3 (rate→key→gate→**write-gate**→body→delegate); **владелец создаваемого ящика — техпользователь `crm-service`** (super_admin, `group_id NULL`, без `telegram_links`, сидируется в lifespan по образцу `seed_super_admin`) — **Q-0039-1 решён аудитом: все каналы доставки (TG/push/webhooks/forwarding) резолвят получателей по `mail_accounts.group_id`/`user_groups`, НЕ по `user_id`** (`telegram_notifications.py:200-217`/`push_notify_dispatch.py:125`/`webhook_dispatch`/`forward_dispatch.py:187`); `leader_user_id` nullable → опция (а) отклонена; следствие — email ящика глобально-уникален для headless-пути (`409`); read-фильтры: `group_id`/`mail_account_id` в `GET /messages` становятся **повторяемыми и AND-комбинируемыми** (`list[int]`, BC одиночного значения; **взаимоисключение ADR-0037 снято — supersede §3**, кода `field=filter` больше нет; эффективный набор = canonical ∩ группы ∩ ящики, пустое пересечение/незнакомый id → пустая страница), `GET /mailboxes` += `is_active: bool\|None` + повторяемый `group_id`, **`ExternalMailboxDTO` += `last_synced_at`/`last_sync_error`/`consecutive_failures`**; без миграции БД. Extends ADR-0029/0035/0037 | accepted | 2026-07-09 |
| [ADR-0042](./ADR-0042-external-team-create-delete.md) | External team (group) create + guarded delete для ленивого провижининга почтовых групп из headless-CRM: `POST /api/external/teams{name}` → `201 {id,name}` создаёт **leaderless**-группу (`GroupsRepo.insert(leader_user_id=None)`, owner/actor=`crm-service`), **НЕ идемпотентно и дубль имени НЕ конфликтит** (`groups.name` не UNIQUE — только `uq_groups_leader_user_id`+`ck_groups_name_length`; реюз/`409` по имени отвергнут — имена штатно коллизируют, связь команда↔группа по `id`; ретрай-безопасность на CRM-стороне: connect-only-ретрай + **CAS на `teams.mail_group_id`** (compare-and-set `WHERE mail_group_id IS NULL`, без лока через сеть) + запись привязки до ящика; остаток — безвредная пустая сирота, реклейм через DELETE, уборка — `TD-048`); `DELETE /api/external/teams/{id}` — guarded-реклейм **пустой** группы в **локальной транзакции с `SELECT ... FOR UPDATE` на строке `groups`** (анти-TOCTOU; лок короткий, без сетевого I/O — легитимен, в отличие от отвергнутого CRM-side row-lock через внешний вызов) (`204`), непустая (ящики `mail_accounts.group_id` FK=`SET NULL` / участники / лидер) → `409 conflict`, нет id → `404 not_found`; оба под `EXTERNAL_WRITE_ENABLED`+`LIMIT_EXTERNAL_WRITE`, auth-flow ADR-0039 §1; `PATCH`(rename) НЕ добавлен (имя косметическое, отложено); без миграции БД. Extends ADR-0029/0037/0039, парный CRM `ADR-043` | accepted | 2026-07-10 |
| [ADR-0040](./ADR-0040-global-tags.md) | Глобальные теги (единый админский каталог): `tags.user_id` → **nullable** (`NULL` = глобальный, применяется ко ВСЕМ письмам), `uq_tags_user_name` остаётся + **partial-unique `uq_tags_global_name (name) WHERE user_id IS NULL`** (новая alembic-ревизия, down_revision `20260706_022`); SQL `backend/app/tags/sql.py` — `APPLY_TAGS_TO_MESSAGE` `JOIN users`→**`LEFT JOIN`** + ветка `t.user_id IS NULL` (иначе глобальные теги выпадали), `APPLY_TAG_TO_EXISTING` глобальный охват через `is_super_admin=TRUE`; идемпотентность `ON CONFLICT (message_id,tag_id) DO NOTHING` и семантика матчинга ADR-0017 (whole-word/case-sensitive/escape) **не меняются**; builtin (`backend/app/tags/builtin.py`) → глобальные, сидирование в **lifespan** (`seed_builtin_tags`, по образцу `seed_super_admin`) вместо ленивого `ensure_builtin_tags` per-login; раздел `/api/external/tags` CRUD от `user_id IS NULL` (под ADR-0039 gate); **регрессия доставки получателям исключена аудитом** (webhooks изолированы от тегов ADR-0023 §3.2, forwarding по группе, TG-получатели по группе); TG-**громкость**: в default `TG_NOTIFY_ALL_MESSAGES=true` не меняется, в опции `false` осознанно растёт (глобальные builtin матчат независимо от логина — исправление скрытого дефекта, §5). Extends ADR-0017/0022 | accepted | 2026-07-09 |
| [ADR-0041](./ADR-0041-disable-jinja-ui.md) | Отключение собственного Jinja-UI (headless-режим, **реализация S5**, отдельно от ADR-0039/0040): в `main.py::create_app` остаются **`health_router` (`/healthz`,`/readyz`)**, **`external_router` (`/api/external/*`)**, **`telegram_router` (`/api/telegram/*`)**; демонтируются HTML-роутеры (`auth`/`messages`/`accounts`/`tags`/`admin`/`groups` UI + `send` form-fallback), `StaticFiles /static`, `templates/`+`static/`, friendly-redirect `→/login`; worker не затрагивается. **Q-0041-1 (решается в S5):** судьба `oauth_router` (Outlook consent человеко-обращён — вероятно остаётся) и CRUD `webhooks`/`forwarding` (нужны `/api/external/*` если управляет CRM — будущий ADR). Extends ADR-0001/0015/0021 | accepted | 2026-07-09 |
| [ADR-0038](./ADR-0038-reversible-login-password-storage.md) | Обратимое хранение пароля входа оператора для показа админом: новая колонка `users.password_encrypted BYTEA NULL` (AES-256-GCM тем же `MAIL_ENCRYPTION_KEY`, AAD-домен `b"user_pw:"+user_id`, миграция `20260706_022` down_revision `20260703_021`) **в дополнение** к argon2-хешу (ADR-0006 — остаётся источником истины входа); админ **задаёт** пароль в `POST /api/admin/users` / `POST /api/admin/users/{id}/reset` (опц. поле `password`) → пишем хеш + обратимую копию; self-set `complete_set_password` тоже пишет обе (без рассинхрона); старые (только хеш) → `password_encrypted=NULL` → колонка «—»; показ — новый **`GET /api/admin/users/{id}/password`** (super_admin, decrypt, `404 password_not_set` при NULL, per-actor rate-limit `LIMIT_ADMIN_PASSWORD_REVEAL` default 30/min, audit `user_password_revealed` на каждый показ, значение НЕ в логи), UI маска+reveal on-demand; **осознанный security-компромисс** (доступ к БД+ключу раскрывает пароли входа) — меры: super_admin-only, audit, no-log, домен-AAD; audit-actions `user_password_set`/`user_password_revealed`; **GET /password не мутация → method-override не нужен, счётчики `_OVERRIDE_*` 7/16 без изменений**; попутно (по ADR-0030, без нового ADR) `additional_group_ids` в `POST /api/admin/users` — мульти-команды при создании `group_member`; **дополняет ADR-0006, reuse AES ADR-0005 §2 / ADR-0030** | accepted | 2026-07-06 |
| [ADR-0043](./ADR-0043-strip-to-connector-push-to-crm.md) | **Агрегатор → чистый mail-connector** (отмена курса headless-прокси): оставляет движок (IMAP-синк, SMTP-send, креды/шифрование) и **сам push'ит новое письмо в CRM** (`POST /api/mail/ingest`, HMAC `CRM_PUSH_SECRET` + timestamp-anti-replay, батч, идемпотентность на CRM по UNIQUE, вложений нет); `messages` → push-outbox (`+pushed_at`, ретенция), очередь ретраев `crm_push_queue` + `crm_push_dispatch`/`crm_push_recovery` (реюз паттерна `tg_notify`); обобщённый **`POST /api/external/mailboxes/{id}/send`** для reply/forward из CRM (заменяет message-scoped reply ADR-0035). **УБИРАЕТ:** теги (ADR-0017/0040 — матчинг переезжает в CRM ПОБУКВЕННО), Telegram (ADR-0018/0022/0024/0027/0033 — SSO/нотификации/4 push-бота/mailbox-alert; **5 ботов переключают вебхуки на CRM**), webhooks (ADR-0023, в проде 0), forwarding (ADR-0034 → в CRM по team), группы/роли/пользователи (ADR-0019/0030/0031 — `mail_accounts.group_id` drop, `user_id`→единый `crm-service`, владение командой только в CRM), external teams/tags (ADR-0037/0039-tags/0042), вложения/MinIO (ADR-0007), Jinja-UI/Mini App (ADR-0041 финал). Парный CRM `ADR-044`. Исполнимый runbook демонтажа — `ADR-0044`. **Supersedes** ADR-0017/0018/0019(groups)/0022/0023/0024/0027/0030/0031/0033/0034/0035/0037/0040/0042; сужает ADR-0039/0041. **§2 (перечень точек hook status-канала) амендирован [ADR-0046](./ADR-0046-mailbox-status-hook-points.md)** — прежние три точки неполны, нормативен перечень H1–H6/H7a/H7b + N1–N7 (реализован полностью, `e7c7b52`). **§3 (контракт обобщённого send) амендирован [ADR-0048](./ADR-0048-external-send-contract-and-reply-restore.md):** ответ — **`200 {smtp_message_id}`**, `sent_id` снят (его единственный источник — `INSERT` в дропаемую `sent_messages`; идентификатор отправки выдаёт CRM из своей `mail_sent_messages`) | accepted (§2 амендирован ADR-0046; §3 — ADR-0048) | 2026-07-10 |
| [ADR-0044](./ADR-0044-decommission-runbook.md) | **Runbook демонтажа** (операционализирует ADR-0043 §4/§5, финализирует ADR-0041): исполнимый необратимый план сноса снятых подсистем + отключения фронта. Инвентаризация 19 таблиц с вердиктами — **KEEP** 3 (`mail_accounts` +drop `group_id`/repoint `user_id`→`crm-service`; `messages` push-outbox; `users` техническая, 1 ряд `crm-service`), **DROP** 16 (`sent_messages`/`sent_attachments`/`attachments`/`tags`/`tag_rules`/`message_tags`/`telegram_links`/`telegram_notifications`/`webhooks`/`webhook_deliveries`/`group_forwarding`/`message_forwards`/`user_groups`/`groups`/`users_settings`/`admin_audit`). FK-безопасный порядок (сначала код-читатели: `sync_cycle` apply_tags/enqueue/MinIO, worker-jobs, `main.py` HTML-роутеры/static/middlewares → потом миграция `mail_accounts` → drop 16 таблиц referencing→referenced → развязка `users.group_id`+drop `groups` → редукция `users`→`crm-service` → MinIO/env). Бэкап перед точкой невозврата (`admin_audit` обязателен — TD-050 + снапшот дропаемых + MinIO bucket `mail-attachments`). Снос фронта: все HTML-URL→404, живы `/healthz`/`/readyz`/`/api/external/*`. **Q-0041-1 решён:** session-`oauth_router` снимается (гейт: готовность external-OAuth-роутов ADR-0045), `OutlookTokenService`+`OutlookOAuthService` остаются/адаптируются. Заведён TD-051; TD-052 (headless re-onboarding) закрыт ADR-0045. Добавлен §9-гейт полноты фазы (import/mypy/тесты — машинное доказательство отсутствия висячих импортов) + §8-таблица детача. Extends ADR-0043/0041/0039. **§7/Phase A3/Phase G амендированы [ADR-0045](./ADR-0045-external-outlook-oauth-headless.md)** (сохранить `OutlookOAuthService` headless + `OUTLOOK_REDIRECT_URI`). **§2/Фаза A2 амендированы [ADR-0048](./ADR-0048-external-send-contract-and-reply-restore.md):** A2 расщеплена на **A2.1** (аддитивная реализация send — чинит живой прод-баг «reply из CRM не работает», деплоится отдельным ранним релизом, без DDL) и **A2.2** (снятие reply/`EXTERNAL_REPLY_ENABLED`/writer'а `sent_messages` — внутри A1+A3-релиза). **§3/§4 Фаза C амендированы §3.1 (self-amendment):** миграция `20260715_025` сама идемпотентно сидит `crm-service` перед repoint'ом (self-sufficient, INSERT-если-нет) вместо `RuntimeError`-зависимости от app-lifespan — устранён CI-блокер DDL (падение `alembic upgrade head` на пустой БД до старта app) и скрытая связность «схема зависит от засиженных приложением данных»; §3.1 также нормирует **`SET CONSTRAINTS ALL IMMEDIATE`** после repoint'а — self-seed ставит отложенное событие FK `users_group_id_fkey` (`DEFERRABLE INITIALLY DEFERRED`), которое в single-transaction alembic-цепочке уронило бы Фазу E `DROP COLUMN users.group_id` с `pending trigger events` | accepted (§2/A2 амендированы ADR-0048; §3/Фаза C — self-amendment §3.1) | 2026-07-10 |
| [ADR-0045](./ADR-0045-external-outlook-oauth-headless.md) | **External Outlook OAuth (headless)** — восстановление consent-flow для добавления/переподключения Outlook-ящиков из CRM (закрывает **TD-052**): `OutlookOAuthService` **остаётся** и адаптируется под headless (`OAuthState{code_verifier, crm_state}`, owner создаваемого ящика = `crm-service`; `insert_oauth_account_with_id` transition-safe — `group_id=None` до drop-миграции демонтажа, опустить аргумент только после снятия колонки); новые external-роуты в сохраняемом `external/router.py` — `POST /api/external/mailboxes/oauth/authorize {crm_state}` (write-gate `EXTERNAL_WRITE_ENABLED`, `outlook_oauth_enabled=false`→`404`) → `{authorize_url, state}`, и `GET /api/external/mailboxes/oauth/callback` (зарегистрированный `redirect_uri`, авторизация одноразовым `state`+PKCE, минимальная self-contained HTML-страница успех/ошибка); после create/relink — **server-to-server HMAC-уведомление CRM** `POST {CRM_OAUTH_INGEST_URL}=/api/mail/oauth/ingest` (реюз `CRM_PUSH_SECRET`, connect-only-ретрай, идемпотентно); env: **НЕ удалять** `OUTLOOK_CLIENT_ID`/`_SECRET`/`OUTLOOK_REDIRECT_URI`(→`{APP_BASE_URL}/api/external/mailboxes/oauth/callback`)/`OUTLOOK_TENANT`/`OUTLOOK_OAUTH_STATE_TTL_SECONDS`, добавить `CRM_OAUTH_INGEST_URL`. Амендмент **ADR-0044 §7/Phase A3/Phase G**, extends ADR-0025/0039. Парный CRM `ADR-045` | accepted | 2026-07-10 |
| [ADR-0046](./ADR-0046-mailbox-status-hook-points.md) | **Статус-канал ящика: исчерпывающий перечень hook-точек + семантика `last_synced_at`** (амендмент `ADR-0043` §2 и `ADR-0026`). Прежняя формулировка «точка hook — `mark_sync_success`/`_disable_after_failures`/`mark_transient_error`» **неполна**; нормирован проверяемый перечень из **восьми** точек — **H1** успешный цикл (`sync_cycle.py:354`→hook `:366`), **H2** `_record_transient`→`mark_transient_error` (`:693`→hook `:703`; покрывает call-site `:574` transient / `:590` permanent-фаза-0 / `:907` unexpected), **H3** `_record_failure`→`mark_sync_failure` (`:706`→hook `:724`, фаза 2), **H4** `_disable_after_failures` (`:770`→hook `:815`), **H5** re-enable по смене кредов (`accounts/service.py:708`), **H6** `set_active` (`:732`), **H7a** переход в `oauth_needs_consent` (`oauth/service.py:657`→hook `:675`, пишет маркер-константу `OAUTH_NEEDS_CONSENT_SYNC_ERROR` в `last_sync_error`), **H7b** clean-skip-ветка воркера (`sync_cycle.py:624`→hook `:741`: guarded-`UPDATE` маркера при `last_sync_error IS DISTINCT FROM` маркера + push **только при фактической записи** — иначе мёртвый ящик остался бы в CRM **зелёным навсегда**; идемпотентно, backfill не нужен — самовосстановление за один цикл синка). Не-hook точки N1–N7 (создание/удаление ящика — инициатор CRM; OAuth-токены; подавленный transient; circuit-breaker; `disable=True`; нестатусные ветки `update_fields`) — с обоснованием. **Инвариант §2:** enqueue **строго после COMMIT**, на call-site (не в repo), через **единый** хелпер `enqueue_crm_status_best_effort` (`crm_push/service.py:187`), best-effort, без дедупа (идемпотентность — CRM `down_alert_sent_at`). **§2.1 (архитектурное правило):** в HTTP-слое доменный сервис статус-событие **НЕ отправляет** — копит id (`accounts/service.py:120`) и роутер флашит `flush_crm_status_events()` **вне** `db.begin()` (`accounts/router.py:319`, `external/router.py:511`), т.к. teardown сессии не коммитит (`shared/db.py:93-112`); push из открытой транзакции для деактивации невосстановим. **§2.1.1:** статус-пишущих методов сервиса ровно два (закрытый перечень — `update` при `creds_changed`, `set_active`); **любой** роутер поверх них ОБЯЗАН звать `flush_crm_status_events()` вне `db.begin()` — забытый flush теряет событие, и сам flush ни исключения, ни лога на это не даёт, грепом по `enqueue_crm_status_best_effort` не ловится; проверки — парный греп `_pending_status_account_ids` (§5 п.4) + **рантайм-детектор** (§5 п.6, `TD-054` **закрыт**): `shared/session_guards.py` на закрытии любой DB-сессии (`shared/db.py:110`/`:127`) даёт `warning` `crm_status_pending_dropped` (прод, запрос не падает) либо `AssertionError` в strict-режиме (под pytest / env `SESSION_GUARD_STRICT`, `07-deployment.md` §4 «Session guards»); детектор не флашит. **§1:** `last_synced_at` = время последней **УСПЕШНОЙ** синхронизации — единственный писатель `mark_sync_success` (`mail_accounts.py:480`); запись `now()` на PERMANENT **отменена** (ломала окно `SYNC_TRANSIENT_SUPPRESS_MINUTES` и показывала сбойный ящик «синканным минуту назад»). Контракт/миграции не менялись. Парная норма CRM — `ADR-044` §3 | accepted — **реализован полностью** (`e7c7b52`; `TD-053` закрыт) | 2026-07-13 |
| [ADR-0047](./ADR-0047-mailbox-test-hard-deadline.md) | **Hard-deadline connection-test ящика (IMAP+SMTP) → доменная `422`, не `504`/зависание** (амендмент `ADR-0013` и `ADR-0039` §1; парная норма CRM — `ADR-053` §5). Прод-баг: CRM обрывала проверку на 10-й секунде, агрегатор отвечал осмысленной `422` через **~31 с** — причина отказа до пользователя не доходила. Нормировано: **единый `asyncio.wait_for` ВНУТРИ обоих probe-методов** (закрытый перечень — **P1** `_test_credentials` (`accounts/service.py:414-430`, тело `:434-472`), **P2** `_test_oauth_account` (`:517-520`); обёртка «по call-site» запрещена — у `_test_credentials` **два** вызывающих, `service.py:371` и `:490`), чем автоматически покрыты `test` / `create` (`service.py:667`) / `PATCH` при смене кредов (`:891`); таймаут — новый env **`MAILBOX_TEST_DEADLINE_SECONDS`** (default **45**, норма границ `ge=10, le=45`); `_IMAP_TIMEOUT`/`_SMTP_TIMEOUT` (`testers.py:57-58`) **30 → 20**, обе SMTP-пробы получили `wait_for`-guard (`+5`: `testers.py:240-250`, `:374-383`), которого у них не было. **Запрещён второй ДЕДЛАЙН на уровне вызывающих, а не всякий `wait_for`** (§1.1): per-probe guards внутри `testers.py` разрешены и предписаны — они не бюджет, а **маркер атрибуции стадии**; существующий IMAP-guard (`testers.py:140-143`) снимать запрещено. **Инвариант (граница probe — дедлайн; граница ОТВЕТА — ТРИ слагаемых, §2.1/§2.3):** `MAILBOX_TEST_DEADLINE_SECONDS 45 + teardown после отмены ≤ 5 + внепробная часть запроса ≤ 5 = 55 < nginx агрегатора 60 = gunicorn --timeout` — доменная `422` JSON обязана всплыть **раньше**, чем прокси превратит ожидание в `504` HTML (тот же класс инварианта, что у отправки, `send/service.py:41-52`). **Teardown-плечо (§2.3):** `asyncio.wait_for` при истечении **дожидается** `finally` отменённой пробы — `await _close_smtp_client(client)` (`testers.py:210-211`, `:355-356`) с вежливым `QUIT` под `_SMTP_QUIT_TIMEOUT = 5` (`:65`); без этого time-box'а `QUIT` против молчащего сервера тянулся бы до `_SMTP_TIMEOUT = 20` (`45 + 20 = 65 > 60`). Машинно охраняется **`le=45`** (формула `le ≤ 60 − teardown 5 − внепробная 5 − резерв 5`; в коде — `shared/config.py:169`, `Field(default=45, ge=10, le=45)`); прежнее `le=50` **отменено амендментом** — оно не учитывало teardown и при значении `50` давало ровно `50 + 5 + 5 = 60` = `proxy_read_timeout` → тот самый `504` (**TD-058 закрыт**). Сумма `20 + 20 = 40` — **бюджетная эвристика fail-fast, НЕ bound** (§2.2): сокет-таймауты `imap_tools`/`aiosmtplib` применяются к отдельным операциям/фазам, а не к сумме логина (композиция guards = `25 + 25 = 50 > 45`); ассертить эту арифметику в тестах **запрещено**. Внешняя цепочка (нормативна в `ADR-053` §1.1/§1.2/§1.2.1/§6, агрегатор её не задаёт): `45 < 60 < CRM read 75 < CRM overall-deadline ОДНОГО вызова 85`, а наружное звено CRM нормировано **по бюджету ЗАПРОСА, а не вызова** (`ADR-053` §1.2.1): `Σ overall всех вызовов запроса + внепробная работа CRM (≤5) < proxy_read_timeout nginx CRM 120`; худший путь CRM (`create` + компенсирующее удаление) = `85 + 15 + 5 = 105 < 120`. Прежнее звено «nginx CRM 90» **отменено** (`ADR-053` §6). Исчерпание дедлайна → **только существующие коды** `imap_login_failed`/`smtp_login_failed` (`422`, `details.detail="timeout"` + `stage`), атрибуция по маркеру стадии; **новый код заводить запрещено** — у CRM он упал бы в fallback `422 unprocessable` (`ADR-053` §2). **§4 (сверх требования CRM):** синхронный `assert_public_host` (`security.py:77`) блокировал event loop на `socket.getaddrinfo` (`:45-54`), а `wait_for` его **не прерывает** → дедлайн без выноса резолва в `asyncio.to_thread` был бы декоративным; введён `assert_public_host_async` (`security.py:104-121`), на него переведены и call-site'ы теста (`testers.py:138,232,298,372`), и **все четыре вне теста** — `send/service.py:228`, `:298`, `:519`, `worker/app/sync_cycle.py:212` (**TD-056 закрыт**). ⚠️ Класс дефекта закрыт **не полностью**: тот же блокирующий `getaddrinfo` живёт во втором резолвере — `shared/url_safety.py:87`, зовётся синхронно из корутин webhook-подсистемы (`webhooks/dispatch_service.py:304`, `webhooks/service.py:86`/`:429`) → долг **TD-057**. Контракт/миграции/бюджет отправки/nginx не меняются. Env-имя `MAILBOX_TEST_DEADLINE_SECONDS` — по конвенции `*_SECONDS` этого репо; в `ADR-053` оно **принято**, правок на стороне CRM не требуется (§5) | accepted — **реализован полностью** (`TD-055`, `TD-056`, `TD-058` закрыты); амендмент teardown-плеча (§2.1/§2.3/§5): `le=50` → **`le=45`** — применён в коде (`shared/config.py:169`) | 2026-07-14 |
| [ADR-0048](./ADR-0048-external-send-contract-and-reply-restore.md) | **Контракт обобщённого send + восстановление reply из CRM** (амендмент ADR-0043 §3 и ADR-0044 §2/Фаза A2; парный CRM `ADR-057`). **Прод-баг:** CRM зовёт `POST /api/external/mailboxes/{id}/send` (`CRM mail_client.py:225`), которого в `external/router.py` **нет** (роуты: `:151`,`:220`,`:280`,`:403`,`:415`,`:428`,`:450`,`:462`,`:551`,`:575`) ⇒ **ответ на письмо сломан с cut-over** (прод: `404` на `/send`, `401` на `/messages/{id}/reply`). **Норма:** ответ send — **`200 { smtp_message_id }`** (`sent_id` **снят**: его производил только `INSERT` в дропаемую `sent_messages`, `send/service.py:460`; идентификатор отправки выдаёт CRM из `mail_sent_messages`, там он `uuid`); запрос — ровно то, что CRM уже шлёт (`{to, cc?, subject?, body_text, in_reply_to?, refs?}`), правок клиента в части запроса нет; `404` от send = **ящик не найден** (не «письма нет») → CRM маппит в `mail_mailbox_not_found`. **Фаза A2 расщеплена:** **A2.1** — аддитивная реализация send (без DDL, ничего не удаляя, деплой немедленно) + парный релиз CRM; **A2.2** — снятие reply/`EXTERNAL_REPLY_ENABLED`/writer'а `sent_messages` в атомарном A1+A3-релизе. **Временный вызов CRM → `/messages/{id}/reply` ЗАПРЕЩЁН** (§5): id писем CRM ≠ id агрегатора после cut-over (CRM вставляет своим `BIGSERIAL` через `ON CONFLICT DO NOTHING` — `nextval` расходуется и на конфликтах при штатных ре-push'ах) ⇒ риск ответа **по чужому письму**; плюс ретенция 30 дней (`cleanup.py:34-40`) и гейт `EXTERNAL_REPLY_ENABLED=false` (`config.py:208`). Reply → send **без потери функции** (threading/дефолты/scope формирует CRM; IMAP-APPEND в «Sent» сохранён реюзом `_send_core`). Заведён `TD-059` (нет consumer-driven контрактной проверки внешнего API; парный CRM `TD-062`) | accepted | 2026-07-14 |
