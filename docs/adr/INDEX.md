# ADR Index

Реестр архитектурных решений (Architecture Decision Records). Формат файла: `ADR-NNNN-<slug>.md`. Каждый ADR содержит секции **Context / Decision / Consequences / Alternatives considered**. Решения иммутабельны: если решение меняется — заводится новый ADR со ссылкой на устаревший, статус старого меняется на `superseded by ADR-XXXX`.

**Open-questions нумерация.** ADR-локальные вопросы используют схему `Q-<ADR>-<seq>` (например, `Q-0029-1`, `Q-WH-*`, `Q-OAUTH-*`) — это **намеренно** локальная нумерация в пределах ADR и **не** попадает в сквозной реестр `99-open-questions.md` (`Q-NNN-N`). Глобальная `Q-NNN` — для cross-cutting вопросов проекта; `Q-<ADR>-<seq>` — для вопросов, закрываемых внутри своего ADR.

| ID | Название | Статус | Дата |
| --- | --- | --- | --- |
| [ADR-0001](./ADR-0001-tech-stack.md) | Базовый технологический стек (FastAPI + PostgreSQL + MinIO + Jinja2) | accepted | 2026-05-05 |
| [ADR-0002](./ADR-0002-imap-library.md) | Выбор IMAP/SMTP-библиотек (imap-tools sync в worker, aiosmtplib async) | accepted | 2026-05-05 |
| [ADR-0003](./ADR-0003-worker.md) | Background worker — APScheduler в отдельном контейнере | accepted | 2026-05-05 |
| [ADR-0004](./ADR-0004-sessions.md) | Сессии — server-side через Redis, opaque cookie | accepted | 2026-05-05 |
| [ADR-0005](./ADR-0005-encryption.md) | Шифрование почтовых паролей — AES-256-GCM с per-record IV | accepted | 2026-05-05 |
| [ADR-0006](./ADR-0006-password-hashing.md) | Хеширование паролей пользователей — argon2id | accepted | 2026-05-05 |
| [ADR-0007](./ADR-0007-storage-scheme.md) | Схема хранения вложений в MinIO | accepted | 2026-05-05 |
| [ADR-0008](./ADR-0008-sync-strategy.md) | Стратегия инкрементальной IMAP-синхронизации (UIDNEXT-based, 30-day initial backfill) | accepted | 2026-05-05 |
| [ADR-0009](./ADR-0009-rate-limiting.md) | Rate-limit на login и password-set | accepted | 2026-05-05 |
| [ADR-0010](./ADR-0010-csrf-protection.md) | CSRF-защита для всех cookie-аутентифицированных POST | accepted | 2026-05-05 |
| [ADR-0011](./ADR-0011-retention.md) | Ретенция писем и вложений — 30 дней, daily cleanup | accepted | 2026-05-05 |
| [ADR-0012](./ADR-0012-message-body-storage.md) | Хранение тел писем — plain text only в PostgreSQL (TEXT) | accepted | 2026-05-05 |
| [ADR-0013](./ADR-0013-concurrency-model.md) | Конкурентность IMAP-сессий — asyncio.Semaphore=10 + thread pool для sync-библиотеки | accepted | 2026-05-05 |
| [ADR-0014](./ADR-0014-logging.md) | Логирование — structlog, JSON в stdout, request_id correlation | accepted | 2026-05-05 |
| [ADR-0015](./ADR-0015-no-js-fallback.md) | No-JS fallback — `_method` override + form-encoded acceptance + content negotiation | accepted | 2026-05-05 |
| [ADR-0016](./ADR-0016-two-step-login.md) | Two-step login (username then password, ADR-style flow as Google) | accepted | 2026-05-06 |
| [ADR-0017](./ADR-0017-tags.md) | Теги для писем — rule-based авто-классификация и пользовательские правила | accepted | 2026-05-07 |
| [ADR-0018](./ADR-0018-telegram-launcher.md) | Telegram launcher bot + WebApp без линковки аккаунтов | partially superseded by ADR-0022 | 2026-05-07 |
| [ADR-0019](./ADR-0019-groups-and-roles.md) | Роли пользователей и группы (super_admin / group_leader / group_member) с visibility-моделью | accepted (§11/A2 «отказ от multi-group» superseded by ADR-0030) | 2026-05-08 |
| [ADR-0020](./ADR-0020-mail-account-nickname.md) | Никнейм (display_name) у mail-аккаунтов | accepted | 2026-05-08 |
| [ADR-0021](./ADR-0021-russian-localization.md) | Полная RU-локализация UI без i18n-фреймворка | accepted | 2026-05-08 |
| [ADR-0022](./ADR-0022-telegram-sso-and-notifications.md) | Telegram persistent SSO (initData HMAC + `telegram_links`) + push-уведомления о письмах с тегами (отменяет «без линковки» из ADR-0018, закрывает TD-013) | accepted | 2026-05-13 |
| [ADR-0023](./ADR-0023-outbound-webhooks.md) | Outbound webhooks для команд (один webhook на `group_id`, AES-GCM secret, dispatcher по паттерну ADR-0022, фильтр «не флудим историей») | accepted | 2026-05-20 |
| [ADR-0024](./ADR-0024-multi-telegram-links.md) | Несколько Telegram-привязок на один аккаунт (снятие `UNIQUE(telegram_links.user_id)`, ключ идемпотентности `telegram_notifications` → `(message_id, telegram_user_id)`, доставка во все живые чаты; расширяет ADR-0022) | accepted | 2026-05-27 |
| [ADR-0025](./ADR-0025-outlook-oauth2.md) | OAuth2 (XOAUTH2) для личных Outlook (consent через сайт+OctoBrowser, IMAP/SMTP XOAUTH2 напрямую, refresh-токен AES-GCM, расширение `mail_accounts`) | accepted | 2026-05-27 |
| [ADR-0026](./ADR-0026-sync-error-resilience.md) | Отказоустойчивость синхронизации: классификация transient/permanent (единая таблица + приоритеты, «too many connections» = transient), transient не дисейблит, circuit-breaker против массового disable, DNS/connect-retry, само-восстановление через `mark_sync_success`; extends ADR-0008 | accepted | 2026-05-28 |
| [ADR-0027](./ADR-0027-push-team-bots.md) | Push-only Telegram-боты по командам (round-31: `ivan`/`alexandra`/`andrei`; **round-44: + `business2` → 4 бота**): отдельная очередь `push_notify_queue` + worker-job `push_notify_dispatch`, маппинг бот→команда явным `group_id` в `.env`, фиксированные `ADMIN_TELEGRAM_IDS`, fire-and-forget без БД-трекинга/recovery; reuse `format_notification`/`send_notification` (токен-параметризация); extends ADR-0022 §2, основной бот не тронут. **round-42:** push-боты получают callback-кнопку «Посмотреть сообщение» + собственный webhook (`/api/telegram/push-webhook/{bot_name}`, per-бот `BOT_*_WEBHOOK_SECRET`); callback авторизуется по `ADMIN_TELEGRAM_IDS` + group-match (§10–§11); токены/secret теперь нужны и в api. **round-44:** 4-й бот `business2` (`BOT_BUSINESS2_TOKEN`/`_GROUP_ID`/`_WEBHOOK_SECRET`, прод `group_id` задаёт оператор в `.env`, ≠1/2/3); роут и диспатчер generic — change только config+redact, новых endpoint/job/миграций нет | accepted | 2026-06-09 (upd round-42; round-44 +business2) |
| [ADR-0028](./ADR-0028-oauth-login-failed-transient.md) | OAuth `Login failed` от Microsoft IMAP = **transient** (контекстная классификация по `auth_type`): refresh успешен ДО IMAP ⇒ `login failed`/`authenticationfailed` у `oauth_outlook` — серверная флуктуация, не permanent (retry + no-disable + suppress); password-поведение неизменно; без миграции, kill-switch `SYNC_OAUTH_LOGIN_FAILED_TRANSIENT`; extends ADR-0026 §1/§2/§4 | accepted | 2026-06-10 |
| [ADR-0029](./ADR-0029-external-pull-api.md) | External PULL-API для B2B-партнёра: `GET /api/external/messages?since_id&limit` — инкрементальный keyset по `messages.id BIGSERIAL` (id ASC, без пропусков/дублей; retention даёт безвредные id-gaps), ВСЕ письма системы (super_admin visibility, без сессии), СЫРОЕ полное тело (`body_text`+`body_html`, без collapse-нормализации), `to_addrs`/`cc_addrs` included, вложения нет; auth static `EXTERNAL_API_KEY` (`compare_digest`, `X-API-Key`/`Bearer`), отдельный версионируемый `ExternalMessageDTO`; без миграции БД | accepted | 2026-06-11 |
| [ADR-0030](./ADR-0030-multi-group-membership.md) | Членство пользователя в нескольких командах (multi-group): аддитивная M:N-таблица `user_groups` — источник истины для видимости/уведомлений; `users.group_id` сохраняется как «домашняя» команда (инвариант лидера, присвоение `mail_accounts.group_id`, роль); видимость консистентна с уведомлениями (член N команд видит ящики/письма и получает TG-уведомления всех своих команд); `mail_accounts.group_id` 1:1 не меняется; добавлять можно всех кроме super_admin; перенос лидера запрещён; endpoints `POST`/`DELETE /api/admin/users/{id}/groups`; push-боты ADR-0027 не тронуты (broadcast по `account.group_id`, не по членству); **supersedes ADR-0019 §11/A2 (partial)** | accepted | 2026-06-23 |
| [ADR-0031](./ADR-0031-mailbox-team-selection.md) | Выбор и смена команды для почтового ящика: опциональный `group_id` в create (`MailAccountCreateRequest`) — без него прежнее поведение (домашняя группа владельца / NULL для super_admin self); перенос существующего ящика через расширение `PATCH /api/mail-accounts/{id}` полем `group_id` (БЕЗ нового form-fallback regex — счётчик `_OVERRIDE_REGEX_PATHS=16` не меняется); единые правила допустимости целевой команды (member — свои `user_groups`/только себе; leader — свои команды себе или своя команда участнику; super_admin — любая/NULL; нарушение → 400/403/404, не 500); transfer запрещён для `group_member`; источник списка — новый `GET /api/my/groups` (reuse `GroupsRepo`); audit `mail_account_group_change`; миграция БД не нужна (`mail_accounts.group_id` уже есть); **extends ADR-0019 §8 / ADR-0030** | accepted | 2026-06-23 |
| [ADR-0032](./ADR-0032-dedicated-server-and-host-certbot.md) | Выделенный prod-сервер Hetzner (`49.12.189.77`, Ubuntu 26.04, домен `postapp.store`) взамен общего `132.243.113.117` + закрепление host-level TLS: `certbot` пакет + `certbot.timer` (webroot `/var/www/certbot`), host `/etc/letsencrypt` bind-mount RO в контейнер nginx — **нет** сервиса certbot и volume `mas_certbot_certs` в compose (устранение docs-дрейфа §2/§6); фактические GitHub Secrets `DEPLOY_HOST`/`DEPLOY_USER=root`/`DEPLOY_KEY_B64`(base64)/`DEPLOY_PATH=/opt/mail-agregator`; GHCR-образы публичные (без `docker login`); runbook переезда — `07-deployment.md` sec. 15 | accepted | 2026-07-01 |
| [ADR-0033](./ADR-0033-mailbox-down-telegram-alert.md) | Telegram-оповещение о нерабочей (авто-отключённой) почте: триггер в единственной точке auto-disable `worker.sync_cycle._disable_after_failures` (ADR-0026 §3; circuit-breaker/transient/needs-consent/ручное отключение НЕ триггерят); идемпотентность «ровно один алерт на переход» через новый столбец `mail_accounts.disabled_alert_sent_at TIMESTAMPTZ NULL` (guarded set `WHERE ... IS NULL` в disable-транзакции; reset в NULL на re-enable); получатели — новый `TelegramNotificationsRepo.list_recipients_for_mailbox(id)` с тем же предикатом видимости (super_admin/членство `user_groups` ADR-0030/owner + живой `telegram_links` + `tg_notifications_enabled`), но БЕЗ per-message-предикатов (`internal_date`/tag); доставка **основным** ботом ADR-0022 через новую очередь `mailbox_alert_queue` + APScheduler-job `mailbox_alert_dispatch`, per-chat dedup, fire-and-forget (TD-042); текст «⚠️ Почта … не работает: …»; миграция `20260703_020`; **extends ADR-0022 §2 / ADR-0026 / ADR-0030** | accepted | 2026-07-03 |
| [ADR-0034](./ADR-0034-leader-mail-forwarding.md) | Переадресация входящих писем команды на почту лидера: per-group конфиг **одна запись на команду** `group_forwarding` (`group_id UNIQUE`, `forward_to`, `is_active`) + дедуп-claim `message_forwards` (`UNIQUE(message_id, group_id)`, claim `INSERT … ON CONFLICT DO NOTHING RETURNING` ДО отправки → exactly-once); миграция `20260703_021`; CRUD `GET/PUT(upsert)/DELETE /api/forwarding/me` + form-fallback (2 exact-пути → `_OVERRIDE_EXACT_PATHS` 5→7, регекс-16 без изменений); пайплайн-форк webhooks — хук в `sync_cycle` (enqueue **всех** новых входящих ящиков команды `group_id IS NOT NULL`, loop-guard на enqueue-side: skip уже несущих `X-Forwarded-By: mail-aggregator`) → `forward_dispatch_queue` → job `forward_dispatch` (temporal-guard `internal_date >= gf.created_at`, loop-guard `forward_to != account.email`, claim, `build_forward_mime` multipart text+html+вложения из MinIO с `FORWARD_MAX_TOTAL_BYTES`-лимитом, oversized пропуск с пометкой) → SMTP **кредами получившего ящика** (`From`=ящик, `To`=лидер, блок «пересланное сообщение»; reuse `smtp_send_message` password/XOAUTH2; **без** Sent-append); только команда (персональные `group_id NULL` не шлём), только новые после включения; fire-and-forget без recovery/retry (TD-043); фиче-флаг `FORWARDING_ENABLED`; audit `forwarding_updated`/`forwarding_deleted`; **форкает ADR-0023, extends ADR-0030 / ADR-0015 / ADR-0025** | accepted | 2026-07-03 |
| [ADR-0035](./ADR-0035-external-reply-endpoint.md) | External reply-endpoint — единственный scoped write во внешнем API: `POST /api/external/messages/{id}/reply` (ответ на существующее письмо тем же `X-API-Key`). Не рушит read-only ADR-0029 — узкая поверхность: только ответ на видимое в pull письмо, отправитель **не выбирается** (`from`=`mail_account` оригинала), нет CRUD/произвольной отправки/`from_account_id`; threading `In-Reply-To`/`References` по `{id}`. Write **opt-in** отдельным флагом `EXTERNAL_REPLY_ENABLED` (default `false`) — существующие read-only-деплои write не получают молча. Отдельный rate-limit `LIMIT_EXTERNAL_REPLY` (`EXTERNAL_REPLY_RATE_LIMIT_PER_MINUTE`, default 30/min per IP, < read 120/min — write дороже/abuse-опаснее). Тело `ExternalReplyRequest{to?,cc?,subject?,body}` (`to` default `[оригинал.from_addr]`, `subject` default `"Re: "+оригинал.subject`, `body` обяз. непустой ≤1 MiB); ответ `ExternalReplyResponse{sent_id,smtp_message_id}` (без `appended_to_sent`); коды 200/400/401/403/404/409/429/502. Backend: новый `SendService.send_external_reply` (canonical-scope резолв → 404, `from`=`original.mail_account_id`, author `user_id`=владелец ящика) поверх общего send-ядра (MIME/SMTP/append **не** дублируются); без миграции БД; **extends ADR-0029, reuse ADR-0034 §5 SMTP-ядра** | accepted | 2026-07-03 |
| [ADR-0036](./ADR-0036-external-backward-pagination.md) | Backward / newest-first пагинация внешнего PULL-API: тот же `GET /api/external/messages` + явный `order` (enum `asc`\|`desc`, default `asc`) и обратный курсор `before_id` (`int`, `ge=1`, только в `desc`). `order=desc` без `before_id` → самые свежие N (`ORDER BY id DESC LIMIT`), с `before_id` → страница `id < before_id` DESC; ответ `desc` — `{messages(id DESC), next_before_id:int\|null (=min(id) или null), has_more}`. Forward ADR-0029 (`asc`/`since_id`/`next_since_id`) **байт-в-байт неизменен** (BC). Режимы взаимоисключающи: `before_id`@`asc`, `since_id`@`desc`, оба курсора вместе → `400 validation_error`. Курсор по монотонному **`id`** (не `internal_date`) — снимает ограничение ADR-0029 «нет newest-first» без silent-loss поздних писем. Auth/rate-limit `LIMIT_EXTERNAL_API`/CSRF-exempt/redact/super_admin-visibility/canonical-дедуп/`ExternalMessageDTO`+`ExternalTagDTO` — те же; **без миграции БД, без новых индексов (reverse-scan по PK `id`), без новых env/флагов**; reply ADR-0035 и read-only ADR-0029 не затронуты; **extends ADR-0029** | accepted | 2026-07-04 |
