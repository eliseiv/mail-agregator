# 100. Known Tech Debt

Реестр осознанных компромиссов и пунктов, отложенных на будущие итерации. Каждый item имеет ID `TD-NNN`, контекст, влияние и приемлемость.

| ID | Краткое название | Контекст | Impact | Severity | Когда адресовать |
| --- | --- | --- | --- | --- | --- |
| **TD-003** | Нет Prometheus-метрик | На первой итерации только structlog JSON в stdout. Подсчёт SLI — grep по логам. | Низкий: scope маленький. | low | Когда понадобится дашборд / алертинг. |
| **TD-004** | Нет orphan-scan для MinIO | Cleanup по retention или delete-cascade — best-effort; возможны "осиротевшие" объекты в MinIO. | Низкий: накопление < 1% данных в год. | low | Когда disk usage начнёт расти быстрее ожидаемого. |
| **TD-005** | UI отправки не поддерживает аттачи | Таблица `sent_attachments` зарезервирована, но в compose-форме нет upload-поля. | Средний: пользователи могут хотеть прикреплять файлы. | medium | Sprint 2+ (если будет запрос пользователей). |
| **TD-006** | Single worker — single point of failure | При падении worker'а sync останавливается. Restart=always покрывает crash, но не bug-loop. | Низкий: scope; ручной recovery приемлем. | low | Если масштабирование потребует двух+ worker — отдельный ADR с координацией задач. |
| **TD-007** | Нет автоматического failover Postgres | Single-host. При падении хоста — manual recovery из backup. | Средний: RTO ~ часы. | medium | Когда требования к доступности повысятся. |
| **TD-008** | Нет CAPTCHA на login | rate-limit + lockout есть, но distributed brute-force с пула IP теоретически возможен. | Низкий: закрытый сервис, фиксированный набор пользователей. | low | Если станет публичным или появится реальный риск. |
| **TD-010** | Нет сохранения IMAP-флагов (read/seen back-sync) | UI mark-read локальный, не синхронизируется обратно в IMAP. | Средний: пользователи могут ожидать sync с другими клиентами. | medium | Если придёт явный фидбэк. |
| **TD-011** | `apply_to_existing` для тегов — синхронный, лимит 100k messages | См. ADR-0017 §7. На текущем масштабе (≤30k messages/user) синхронное применение приемлемо (<5 сек), но при росте упрётся в HTTP-таймаут 30 сек. Лимит 100k защитит от висящих запросов. | Низкий на старте, средний при росте. | low | При росте до >50k messages у любого user'а — переделать на background worker job + UI status polling. |
| ~~**TD-012**~~ | ~~Pattern в tag_rules не escape'ит `%`/`_` (ILIKE wildcards)~~ | **Снят round-23 (TD-021)**: `*_contains` больше не использует ILIKE. Pattern теперь экранируется через `regexp_replace` и матчится whole-word через `~` + `\y`. `%`/`_` интерпретируются буквально (как обычные символы), wildcard-побочный-эффект исчез. | — | — | — |
| ~~**TD-021**~~ | ~~`*_contains` теги — семантика изменена с substring-case-insensitive на whole-word **case-SENSITIVE**~~ | **Закрыт round-27 (формализован в ADR-0017 §4/§5/§7 + История изменений).** round-23 перевёл `*_contains` с substring-ILIKE на whole-word case-sensitive (`~`, экранированный паттерн). architect-задача «формализовать в ADR» выполнена: ADR-0017 §4 переписан, UI-примечание про `%`/`_`-wildcard убрано (теперь литералы), добавлено «совпадение регистрозависимое, по целым словам». **Важно:** реализация границ через `\y` оказалась багом и заменена на граничные классы в round-27 — см. TD-023. | — | — | — |
| ~~**TD-023**~~ | ~~Границы слова через `\y` ломают паттерны, обрамлённые пунктуацией~~ | **Закрыт round-27.** `\y` = граница «словесный↔несловесный»; паттерн, начинающийся/заканчивающийся пунктуацией (`Congratulations!`, `…requires your attention.`), не матчился — после `!`/`.` идёт пробел/конец (обе стороны несловесные, границы нет). Ломало теги «Реджект»/«Релиз» (особенно `match_mode='all'`, где один несработавший rule снимает весь тег). В `backend/app/tags/sql.py` `\y…\y` заменено на явные граничные классы `'(^|[^[:alnum:]_])' \|\| <escaped> \|\| '([^[:alnum:]_]\|$)'` во всех 8 местах (2 запроса × any/all × subject/body/sender). Гарантии whole-word/case-sensitive сохранены. Формализовано в ADR-0017 §4.1. | Функциональный баг (теги не навешивались) — устранён. | — | — |
| **TD-022** | `norm()` + `strip_tags(body_html)` выполняются на каждой проверке rule без индекса | round-27: чтобы многословные `*_contains` матчились на реальных телах (где `html2text`/`text-plain` дают `\n`, прогоны пробелов, U+00A0 внутри предложений — замер: 148/148 писем с `\n`, 108/148 с прогонами, 17/148 с nbsp), обе стороны сравнения проходят `norm(x)=regexp_replace(translate(x,chr(160),' '),'\s+',' ','g')`. **round-29:** `body_contains` теперь дополнительно прогоняет `strip_tags(body_html)=regexp_replace(body_html,'<[^>]+>',' ','g')` (до ~1 MiB на строку) → на full-scan apply-to-existing per-row CPU тела удваивается. Это per-row CPU без функционального индекса по нормализованному `body_text`/`body_html`. См. ADR-0017 §4.2/§4.3. | Низкий: на ≤750k писем и batch ≤50 укладывается в sync-window; runaway-guard 100k и seq-проверка по `tag_rules` доминируют. | low | При росте до >50k писем/user с заметным временем apply — материализованная колонка `messages.body_searchable` (`body_text` + `strip_tags(body_html)`, + trigger) либо expression-индекс под GIN/pg_trgm. Совместить с пересмотром TD-011 (async apply). |
| **TD-024** | `strip_tags(body_html)` не декодирует HTML-сущности | round-29 (ADR-0017 §4.3): `body_contains` матчит дополнительно по `strip_tags(body_html)=regexp_replace(body_html,'<[^>]+>',' ','g')` — снимает только теги `<…>`, но **не** декодирует сущности (`&amp;`→`&`, `&#39;`→`'`, `&nbsp;`→U+00A0). Паттерн, совпадающий с фразой, где в html стоит сущность (напр. паттерн `AT&T` против html `AT&amp;T`), через html-ветку не сматчится. `&nbsp;` частично смягчён `norm()` — но только если сущность уже декодирована в U+00A0; сырой `&nbsp;` остаётся литералом. Для подтверждённого Apple-кейса (письма id 265/381) фраза чистая — фикс работает. | Низкий: типовые триггерные фразы Apple/App Store без сущностей; затрагивает только html-ветку (body_text-ветка не страдает). | low | Если появятся пользовательские паттерны с `&`/кавычками/nbsp, не матчащиеся из-за сущностей — добавить unescape в SQL (цепочка `replace` для топ-сущностей `&amp;`/`&lt;`/`&gt;`/`&quot;`/`&#39;`/`&nbsp;`) или вынести strip+unescape в `html2text` на стороне воркера в материализованную колонку (см. TD-022). |
| ~~**TD-013**~~ | ~~Нет push-уведомлений в Telegram о новых письмах~~ | **Закрыт ADR-0022**. Реализован полный flow: линковка через `telegram_links` + диспатчер `worker.tg_notify_dispatch` + opt-out через `users_settings.tg_notifications_enabled` + idempotency через `telegram_notifications`. | — | — | — |
| ~~**TD-014**~~ | ~~Имя env-var для bot-токена расходится между кодом и docs (`BOT_TOKEN` vs `TELEGRAM_BOT_TOKEN`)~~ | **Закрывается одновременно с ADR-0022 migration**: `shared/config.py` переименовывает `BOT_TOKEN` → `TELEGRAM_BOT_TOKEN` (alias на переходный период), `.env.example` синхронизируется. Devops применяет на prod в том же спринте. | — | — | — |
| **TD-018** | Нет anti-replay set'а для `init_data` в SSO | См. ADR-0022 Open question Q-001-1 и §1.4 «Replay украденного init_data». Текущая защита — TTL 5 минут. Внутри окна, теоретически, replay возможен (требует MitM между Telegram-edge и backend). Anti-replay set (`tg_seen:{auth_date}:{hash[:8]}` в Redis TTL 5min) сверху TTL — отложен. | Низкий: scope (≤5 users); требует cooperating MitM, что не realistic для нашего деплоймента (TLS до nginx). | low | Если появится реальный риск (например, прокси-логика leak'нула токен) — добавить SET-NX перед HMAC-валидацией в `TelegramSSOService.validate_init_data`. |
| **TD-015** | Per-group tags (теги общие для группы) | См. ADR-0019 §11 + §7.4. На текущей итерации теги остаются per-user (ADR-0017). В UX группы это даёт quirk: лидер видит письма участника со «своими» тегами не применёнными (теги — атрибут владельца ящика, не зрителя). | Средний: непривычный UX в режиме группы. Функционально не блокирует. | medium | После валидации UX модели групп: либо `tags.group_id NULLABLE` (общие теги для группы), либо «копировать теги лидера всем участникам» при добавлении в группу. Отдельный ADR. |
| **TD-016** | i18n framework для UI | См. ADR-0021. Сейчас весь UI на русском с error-code mapping в Jinja-macro `error_text(code)`. Если когда-то понадобится EN-вариант (выход на международный рынок) — придётся ретроактивно поднимать gettext/babel и переписывать ~15 шаблонов. | Низкий: продукт сейчас однозначно RU-only, единственный язык. | low | При появлении явного запроса на EN/ES/etc — отдельный ADR с миграцией на gettext/babel + extraction script + CI-step. |
| **TD-017** | Назначение лидера в существующей пустой группе | См. ADR-0019 §5 + §11. Endpoint `PATCH /api/admin/groups/{id}/leader` (либо аналогичный flow в `PATCH /api/admin/users`) не реализован. На сегодня «новый лидер всегда auto-create'ит группу»; перенос лидерства между группами — через delete+create. | Низкий: продукт явно не запросил такой flow. На малом масштабе (≤3 групп) приемлемо. | low | При появлении сценария «передать лидерство без удаления группы» — отдельный ADR + endpoint с revoke_all_for_user старого лидера + audit `user_role_change`. |
| ~~**TD-025**~~ | ~~recovery_scan Telegram-нотификаций не дублирует visibility/opt-out/`internal_date`-фильтры из §2.2~~ | **Закрыт round-33 (ADR-0022 §2.8).** Исправляя CRITICAL-баг частичной доставки (per-message `NOT EXISTS` терял throttled-получателя при наличии другого доставленного — TD-027), recovery-SQL был переписан на per-recipient и теперь **переиспользует recipient-логику §2.2**: JOIN visibility (super_admin/group/owner) + `telegram_links` (`dead_at IS NULL`, `m.internal_date >= tl.created_at`) + opt-out (`COALESCE(us.tg_notifications_enabled,true)=true`) + условный тег-предикат, с `NOT EXISTS (tn WHERE message_id=m.id AND user_id=u.id)`. recovery стал visibility-aware → холостых enqueue писем без получателей / до привязки больше нет. Follow-up «вынести visibility в общий фрагмент» выполнен. | — | — | — |
| **TD-026** | Глобальный Bot API rate-limit (~30 msg/sec на всех чатах) не throttled явно | round-31 (ADR-0022 §2.9): per-chat троттлинг (`TG_SEND_PER_CHAT_PER_MINUTE`) ограничивает поток в один chat_id, но глобального лимита на суммарный исходящий поток бота нет. Смягчено `TG_NOTIFY_BATCH_SIZE=30`/тик при `TG_NOTIFY_DISPATCH_INTERVAL=5s` (~6 msg/sec глобально, ниже 30/sec) + backoff на `429` (rollback + re-enqueue). | Низкий: текущая batch/interval-конфигурация держит глобальный поток заведомо ниже лимита Bot API; всплеск при `TG_NOTIFY_ALL_MESSAGES=true` + много активных чатов теоретически возможен, но `429` обрабатывается re-enqueue без потери. | low | Если на проде будут стабильные `429` — добавить глобальный token-bucket (`rl:tg_send_global`, ~25/sec) проверкой в диспатчере перед каждым `send_notification`. |
| **TD-027** | При **устойчивом** per-chat throttle часть TG-нотификаций может не доставиться в окне 24ч | round-32 (заведён), **переформулирован round-33** (ADR-0022 §2.9). Причина недоставки — **устойчивый throttle** (`inflow > TG_SEND_PER_CHAT_PER_MINUTE` дольше окна), а НЕ маскированная частичная доставка (последнее было отдельным CRITICAL-багом per-message recovery, исправлено round-33 — см. §2.8, TD-025). Механизм: throttled-получатель пропускается без hot re-enqueue (`continue`, строки `(message_id,user_id)` нет); доставку берёт per-recipient `recovery_scan` (раз в час, окно `TG_NOTIFY_RECOVERY_WINDOW_HOURS=24`). При устойчивом флуде backlog рассасывается ~capacity попыток/чат/час; письмо, не успевшее пройти throttle до выхода за 24ч, recovery перестаёт подбирать → TG-нотификация по нему не доставится. Само письмо НЕ теряется (в БД, видно в UI) — теряется только уведомление. Осознанный компромисс: альтернатива (немедленный hot re-enqueue) давала busy-loop + неограниченный рост `tg_notify_queue` + амплификацию recipient-SQL каждые 5с. | Низкий: только при устойчивом флуде в один chat дольше суток (нетипично для ≤5 users; super_admin может выключить `tg_notifications_enabled` или откатить `TG_NOTIFY_ALL_MESSAGES`). Письмо доступно в UI. | low | Если устойчивый флуд станет реальным — поднять `TG_SEND_PER_CHAT_PER_MINUTE` и/или `TG_NOTIFY_RECOVERY_WINDOW_HOURS`, либо ввести приоритетную/persistent per-recipient очередь с явным счётчиком попыток вместо опоры на recovery-окно. |
| **TD-019** | Outbound webhook rotate-secret — instant cut без grace-периода | См. ADR-0023 Open question Q-WH-1 и §2.5. После `POST /api/webhooks/me/rotate-secret` старый secret становится недействителен немедленно — receiver-side приложение, не успевшее принять новый secret, видит unauth-error до момента deploy с новым env. Двойного secret (старый ещё валиден M минут) в MVP нет. | Низкий: ротация редкая операция, обычно координируется ручно (сообщение «обновите secret»). На малом масштабе (≤5 команд) приемлемо. | low | Если поступит запрос с обоснованием «нужен hot-rotate без downtime» — добавить колонку `webhooks.secret_previous_encrypted` + `secret_previous_valid_until: timestamptz`; dispatcher шлёт оба header'а (`X-Webhook-Secret` + `X-Webhook-Secret-Previous`) на промежуточный период. Отдельный ADR (`ADR-0023.1`). |
| **TD-028** | Legacy-строки `telegram_notifications` с `telegram_user_id=0` после миграции ADR-0024 | Миграция `20260527_017` (ADR-0024 §8 шаг 4): исторические доставки, чей `telegram_links` уже удалён на момент миграции, backfill'ом не получают реальный `chat_id` → ставится синтетический `0` для выполнения NOT NULL. Эти строки уже доставлены (`sent_at` стоит) и нужны только как след. | Низкий: единичные строки; самоустраняются retention-каскадом от `messages` за `RETENTION_DAYS` (30). | low | Не требует действий — рассосётся за 30 дней. Если миграция применяется на свежей базе без истории — items не возникают вовсе. |
| **TD-029** | Per-account proxy для OAuth/IMAP не реализован | ADR-0025 §1/§5: поле `mail_accounts.proxy_url` заложено, но worker/тестеры его игнорируют — IMAP/SMTP-трафик идёт напрямую с сервера (как и просил пользователь для текущей итерации). | Низкий: текущее требование — сбор напрямую. Прокси понадобится только если Microsoft начнёт rate-limit'ить по IP сервера. | low | При появлении потребности — реализовать прокси-коннект (httpx proxies + IMAP/SMTP через SOCKS); отдельная задача, поле уже есть. |
| **TD-030** | XOAUTH2 в `imap-tools`/`aiosmtplib` — через внутренние API | ADR-0025 §4/§Consequences: ни `imap-tools`, ни `aiosmtplib` не дают first-class XOAUTH2; реализуем через нижележащий `imaplib.authenticate("XOAUTH2", …)` (`MailBox.client`) и SMTP `AUTH XOAUTH2`-команду. Хрупко к версиям библиотек. | Средний: апгрейд библиотек может сломать механизм; покрывается интеграционными тестами с mock IMAP/SMTP. | medium | При апгрейде `imap-tools`/`aiosmtplib` — прогнать XOAUTH2-тесты. Если API сломается — зафиксировать версию или перейти на голый `imaplib`/`smtplib`. |
| **TD-031** | Outlook OAuth e2e не проверен на реальном Azure App | ADR-0025 Q-OAUTH-3 (БЛОКЕР e2e): требует `client_id`/`secret` от пользователя + подтверждение, что personal accounts выдают IMAP/SMTP XOAUTH2-доступ и нужные scopes. Код + unit/integration-тесты с моками token endpoint реализуемы без этого. | Средний: до e2e нельзя гарантировать работу на проде. | medium | После получения Azure App от пользователя — провести e2e с реальным личным Outlook-ящиком в OctoBrowser-профиле. |
| **TD-032** | Нет per-provider throttling / INUSE-aware concurrency | ADR-0026 §Alternatives п.5: «too many simultaneous connections»/`INUSE` лечится как transient (не дисейблит) + connect-retry, но **причина** (слишком много параллельных соединений к одному провайдеру) не устранена — глобальный `Semaphore(MAX_CONCURRENT_IMAP)` не группирует по `imap_host`. При большом числе ящиков одного провайдера возможны повторные transient-ошибки. | Низкий: transient не теряет аккаунт, письма подберутся следующим циклом; на текущем масштабе провайдеры не отдают INUSE устойчиво. | low | Если на проде появятся устойчивые «too many connections» к одному провайдеру — ввести per-host под-семафор (напр. `{host: Semaphore(N)}`) или token-bucket per `imap_host`. |
| **TD-033** | Нет DNS-фолбэка/кэша на уровне worker-контейнера | ADR-0026 §Alternatives п.6: единичный DNS-глюк сглаживается `SYNC_CONNECT_RETRIES` на уровне приложения, но инфраструктурного смягчения (локальный resolver/кэш в docker-compose, напр. dnsmasq, либо `dns`/`extra_hosts` в compose) нет. При деградации DNS хоста дольше retry-окна все коннекты падают (теперь — как transient, без disable). | Низкий: код-уровень retry + transient-no-disable покрывают сценарий инцидента; письма не теряются. | low | Если DNS-глюки станут частыми — добавить кэширующий resolver в compose (devops) или `extra_hosts` для известных провайдеров. Ортогонально коду ADR-0026. |
| **TD-034** | Нет полей наблюдаемости `transient_failures` / `disabled_reason` в `mail_accounts` | ADR-0026 §6 / §Alternatives п.2: для MVP полей `is_active`/`consecutive_failures`/`last_sync_error`/`last_synced_at` достаточно (transient просто не трогает счётчик; причина disable есть в audit `account_auto_disabled.details.reason`). Отдельные колонки дали бы метрику «сколько transient-сбоев у ящика» и явную причину disable без обращения к audit, но потребовали бы миграции. | Низкий: наблюдаемость обеспечивается логами + audit; функционально не нужно. | low | Если понадобится дашборд по transient-сбоям/причинам disable (вместе с TD-003 Prometheus) — добавить `transient_failures INT DEFAULT 0` + `disabled_reason TEXT` миграцией; обновить `mark_transient_error`/`_disable_after_failures`. |
| **TD-035** | Нет эскалации при затяжном массовом permanent (circuit-breaker вечно подавляет disable) **+ затяжной transient OAuth-`login failed`** | ADR-0026 §3 + [ADR-0028](./adr/ADR-0028-oauth-login-failed-transient.md) §4. **(а)** Если массовый permanent держится много циклов подряд, брейкер срабатывает каждый цикл и **никогда не дисейблит** реально протухшие/заблокированные аккаунты (блокировка IP воркера / массовый отзыв токенов) — аккаунты вечно долбят провайдера, disable вечно подавляется (нет escape-hatch). **(б, ADR-0028):** OAuth-аккаунт, у которого IMAP **устойчиво** отдаёт `login failed` при валидном refresh (не флак, а реально сломавшийся IMAP), теперь классифицируется как transient → **никогда не дисейблится**, вечно ретраит раз в 5 мин. В обоих случаях `last_sync_error` пишется (оператор видит причину в UI при устаревании `last_synced_at`), но proactive-алерта нет. | Средний: при затяжном внешнем блоке / устойчивом OAuth-IMAP-сбое оператор не получает явного сигнала к ручному вмешательству; миграции не требует. Письма не теряются. | medium | Счётчик подряд-срабатываний брейкера (Redis `sync:breaker_streak`, без миграции); при `>= SYNC_BREAKER_CONSECUTIVE_ALERT` (новый config, предлагаемый default 6 ≈ 30 мин при 5-мин цикле) — audit/alert `sync_breaker_persistent` (severity error). Аналогичный per-account «N подряд transient-циклов» счётчик может покрыть случай (б) для OAuth. |
| **TD-036** | Подтвердить `max_instances=1, coalesce=True` для APScheduler-job `sync_cycle` | ADR-0026 §4 (MINOR-1). DNS/connect-retry в `imap_fetcher` может растянуть `sync_cycle` дольше `SYNC_INTERVAL_MINUTES`. Защита от наложения тиков — `max_instances=1, coalesce=True` на job `sync_cycle` (для `force_sync_dispatch` и tg/webhook-job'ов это уже задокументировано). Нужно подтвердить, что именно `sync_cycle` сконфигурирован так же в `worker/app/main.py`. | Низкий: при штатных таймаутах цикл укладывается в интервал; перекрытие маловероятно. | low | Проверить/выставить `max_instances=1, coalesce=True` для job `sync_cycle` в `worker/app/main.py`; добавить регрессионный тест конфигурации job'ов. |
| **TD-039** | Web-вью письма (`message_view.html`) не схлопывает вертикальные пробелы от spacer-ячеек таблиц | ADR-0022 §2.10 round-39. Фикс «множества пустых строк» round-39 закрыл **TG** «Посмотреть сообщение» (там таблицы стрипаются санитайзером → схлопываем `collapse_blank_lines_tg`). В **web** `body_html \| safe` таблицы рендерятся целиком (намеренно — богатый просмотр); Apple/marketing spacer-`<tr>`/`<td>` со whitespace-содержимым дают вертикальные пробелы, которые `collapse_blank_lines_html` (round-37, чистит только `<p>/<div>/<br>`) не убирает. Web — вторичный канал; основной просмотр у пользователя — TG. | Низкий: косметика во вторичном канале; контент читаем, артефакт — лишняя высота. | low | Если web-артефакт станет приоритетным — расширить `collapse_blank_lines_html` правилом «удалять `<tr>`/`<td>`, чьё единственное содержимое — whitespace/`&nbsp;`/spacer-`<img>`», либо CSS-ограничение высоты пустых блоков. Отличить spacer-ячейку от контентной без рендера нетривиально — действовать осторожно. |
| **TD-041** | Push-only боты по командам (ADR-0027) теряют уведомление при рестарте worker (fire-and-forget) | ADR-0027 §5. Канал push-ботов `ivan`/`alexandra`/`andrei`/`business2` (round-44) доставляет **fire-and-forget**: нет `telegram_notifications`-трекинга, idempotency, recovery-scan, re-enqueue на 429/transient. Если worker падает/рестартует между `LPOP push_notify_queue` и `sendMessage` — item теряется; при `429`/`5xx`/network item логируется и дропается (без повтора). **Само письмо НЕ теряется** (в БД, видно в Inbox, дублируется основным ботом ADR-0022 с полным recovery) — теряется только дублирующее push-уведомление в admin-канале. Осознанный trade-off: idempotency не нужна (повторов нет), recovery избыточна (admin-мониторинг, не критичная доставка). | Очень низкий: admin-канал-«второй экран»; редкие единичные пропуски при рестарте на малом потоке (≤2 admin). Письмо доступно в основном боте и UI. | low | Если оператор сообщит о значимых пропусках — добавить лёгкий трекинг (`push_notifications(message_id, bot_group_id, admin_chat_id)` UNIQUE) + recovery-scan по образцу ADR-0022 §2.8, либо перенести доставку под идемпотентный основной диспатчер с per-bot ключом. Отдельный ADR. |
| **TD-042** | Telegram-алерт об авто-отключённой почте (ADR-0033) — fire-and-forget, без recovery | ADR-0033 §4/§Consequences. Канал `mailbox_alert_dispatch` доставляет **best-effort**: нет реестра доставок (в отличие от `telegram_notifications` для писем), нет recovery-scan, нет re-enqueue на `429`/`5xx`/network (item дропается с логом). Штамп `disabled_alert_sent_at` ставится в disable-транзакции → при падении worker между COMMIT disable и `LPOP mailbox_alert_queue` алерт теряется без повтора. Идемпотентность («один на переход») сохраняется by design. **Само отключение НЕ теряется** — ящик виден в UI как `is_active=false` + `last_sync_error`; алерт лишь проактивный сигнал. Осознанный trade-off (как TD-041 для push-ботов): для операционного сигнала recovery избыточна. | Очень низкий: редкие единичные пропуски при рестарте/сетевом сбое; состояние ящика всегда видно в UI. | low | Если оператор сообщит о значимых пропусках — добавить лёгкий реестр (`mailbox_alert_deliveries(mail_account_id, telegram_user_id, disabled_at)` UNIQUE) + recovery-scan по образцу ADR-0022 §2.8. Отдельный ADR. |
| **TD-040** | `strip_invisible_padding` удаляет U+200E (LRM) / U+200F (RLM) глобально — теоретическая потеря bidi-направления в подлинно-RTL письмах | ADR-0022 §2.10 round-40. Для фикса строки-спейсера marketing-писем (Glassdoor `id=1264`) LRM/RLM добавлены в `_INVISIBLE_PADDING_CODEPOINTS` и удаляются во всех вью (TG/web/preview) и при ingest `body_text`. В подавляющем большинстве трафика это marketing-спейсеры, но в **подлинно** RTL-письмах (арабский/иврит) LRM/RLM изредка задают направление смешанного LTR/RTL-фрагмента — безусловное удаление может сместить визуальное направление куска текста. Хранимый `body_text` (ingest-strip) и render-вью `body_html`/`body_text` будут без маркеров; хранимый `body_html` маркеры сохраняет. | Очень низкий: пользователи — ru/en-mail; направление абзаца в plain-ish TG/web-вью определяется первым сильным символом контента (Unicode bidi base direction), не явными маркерами. | low | Если появится реальный RTL-трафик с искажённым направлением — перейти на bidi-aware-обработку (сохранять LRM/RLM вне спейсер-прогонов) либо удалять Cf только в составе whitespace-spacer-строк перед collapse (точечно, не глобально в `strip_invisible_padding`). |
| **TD-043** | Переадресация писем лидеру (ADR-0034) — fire-and-forget после claim, без retry/recovery | ADR-0034 §3.6/§Consequences. Канал `forward_dispatch` дедуплицирует через `message_forwards` (UNIQUE `message_id,group_id`, claim ДО отправки), но **не** ретраит и **не** имеет recovery-scan (в отличие от webhooks ADR-0023 §3.5). При ошибке SMTP claim-строка получает `error` и больше не повторяется (at-most-once после claim); при падении worker между COMMIT claim и отправкой форвард по письму теряется. **Само письмо НЕ теряется** — оно в БД, видно в Inbox; теряется только его копия в почте лидера. Осознанный trade-off (как TD-041 push-боты / TD-042 mailbox-alert): для нотификационной пересылки recovery избыточна. | Очень низкий: редкие единичные пропуски при SMTP-сбое/рестарте на малом потоке; письмо доступно в UI сервиса. | low | Если лидеры сообщат о значимых пропусках — либо (а) rollback claim-строки на transient SMTP-ошибке + recovery-scan `NOT EXISTS(message_forwards WHERE sent_at IS NOT NULL)` с 24-часовым окном по образцу ADR-0023 §3.5, либо (б) ограниченный per-message retry-счётчик. Отдельный ADR. |
| **TD-044** | `tests/integration/test_send_service.py` — устаревшая фикстура (сигнатуры до VisibilityScope-рефактора) | Pre-existing: 8 ERROR в наборе из-за фикстуры, вызывающей `UsersRepo.create(is_admin=...)` и `send(user_id=...)` — сигнатуры, не приведённые к текущему scope-рефактору (`VisibilityScope`). Тесты **предсуществующие**, не связаны с reply-endpoint ([ADR-0035](./adr/ADR-0035-external-reply-endpoint.md)) и **вне его scope**. `blame: test` (расхождение теста, а не кода/docs) — фичу не блокирует. | Низкий: 8 ERROR в integration-наборе `send`-сервиса; сама функциональность `send`/reply не затронута, зелёный прогон нового reply-набора не зависит от этих тестов. | low | **qa** обновляет фикстуру под текущие сигнатуры: `UsersRepo.create(...)` без `is_admin` и `send(...)` с актуальными параметрами `VisibilityScope`. Отдельная qa-задача, код/контракт не меняются. |
| **TD-045** | Write-репозитории не гарантируют активную транзакцию (`add`+`flush` без begin-инварианта) | Hardening, предложено reviewer'ом при ревью фикса транзакции reply ([ADR-0035](./adr/ADR-0035-external-reply-endpoint.md)). `SentMessagesRepo.insert` и др. write-методы делают только `add`+`flush` без гарантии активной внешней транзакции. Новый write-эндпоинт легко забыть обернуть в `async with db.begin():` — что **уже привело к багу**: reply-endpoint отдавал `200` с недолговечным `sent_id` (вставка откатывалась вне транзакции → «успешный» ответ по факту без durable-записи). Мотивирующий прецедент — баг транзакции reply (ADR-0035). | Средний класс бага: молчаливая потеря записи (`200` при откатившейся вставке) при добавлении любого нового write-роутера без явной транзакции; не единичный дефект, а повторяемый паттерн-риск. | medium | Добавить инвариант — write-операции репозиториев выполняются **только** в активной транзакции: либо `assert db.in_transaction()` в write-методах (`insert`/`update`/`delete`), либо единый транзакционный декоратор / FastAPI-зависимость для write-роутеров, оборачивающая handler в `async with db.begin():`. **backend**-задача; при выборе паттерна декоратора/зависимости — отдельный ADR. Предотвращает целый класс бага (cross-ref ADR-0035 как прецедент). |
| **TD-046** | Латентный дрейф `group_not_found`: спец-код осел в `message`, `code` остался общим (внутренние admin/groups эндпоинты) | Тот же паттерн, что исправлен для mail-account team-selection (`accounts/service.py:388`): спец-код ошибки передан строкой-аргументом в общее исключение и осел в `message`, тогда как `error.code` остался общим (`DomainError.code` — класс-атрибут, конструктор принимает только `message`). 13 сайтов (проверены reviewer): `groups/service.py` 144/147/251/432/478/486/512/515 — 8× `NotFoundError("group_not_found")` → факт. `code="not_found"`, при docs `04-api-contracts.md:841/846/867`, обещающих `404 group_not_found` для `GET/PATCH /api/admin/groups/{id}`; `admin/service.py` 207/221/425/547/574 — `ValidationError("group_not_found", …)` → `code="validation_error"` (400), при docs `645/649`, обещающих `400 group_not_found`. **Оговорки:** (1) сайты **НЕ достижимы с external-пути** — `external/write_service.py` переиспользует только `MailAccountService`/`TagsService`; `groups/service.py` и `admin/service.py` на external не импортируются → текущую headless-поставку (ADR-0039/0040) не ломают; (2) `groups/service.py:251` и `:432` несут комментарий `# hide endpoint existence` — **намеренная анти-энумерация**, смена кода там может быть НЕверна (аудит обязан различать дефект и намеренное сокрытие); (3) весь внутренний admin/UI демонтируется в [ADR-0041](./adr/ADR-0041-disable-jinja-ui.md) (S5) → часть дрейфа исчезнет вместе с ним. Норма аудита — правило shared-блока v3 «Contract-value = поле ответа, а не наличие строки». | Низкий: только внутренние admin/groups эндпоинты (человеко-UI), недостижимы с external; HTTP-статус верный (404/400), но `error.code` общий вместо специализированного → docs↔код расхождение по полю `code`. | low | После стабилизации [ADR-0041](./adr/ADR-0041-disable-jinja-ui.md) (демонтаж внутреннего UI) — переоценить остаток. Для сохраняемых JSON-эндпоинтов: где docs обещают `group_not_found`, поднимать `GroupNotFoundError` (`code="group_not_found"`, уже существует — `exceptions.py:235-239`) вместо `NotFoundError("…")`; для `admin/service.py` — выделенный код либо привести docs к `validation_error`. Анти-энумерационные сайты (`251`/`432`) НЕ трогать без явного решения. Аудит по правилу v3. |
| **TD-047** | Ящик без `group_id` (unassigned-пул, owner `crm-service`) не даёт Telegram-уведомлений | Ящик, созданный админом из CRM **без** `group_id` (headless external write, [ADR-0039](./adr/ADR-0039-external-write-api.md); триггер — CRM `ADR-038`), принадлежит техпользователю `crm-service` (super_admin, `group_id=NULL`, без `telegram_links`). Получатели письма резолвятся `list_recipients_for_message` (`backend/app/repositories/telegram_notifications.py`, строки ~206-219) по `mail_accounts.group_id` → `user_groups` + ветка владельца `u.id = ma.user_id`; `crm-service` ветку владельца не проходит из-за INNER JOIN на `telegram_links` (их у него нет). Итог: по такому ящику **ни один пользователь не получит Telegram-уведомление** (кроме super_admin с живым `telegram_links` — ветка `u.role='super_admin'`). Аналогично — mailbox-alert (`list_recipients_for_mailbox`); webhooks/forwarding при `group_id=NULL` вообще без получателей (резолв по группе). Формально by design (не-админ обязан указать группу — external-валидация), но для админа — **тихая потеря доставки**. | Низкий: unassigned-пул нетипичен; письма НЕ теряются (в БД/ленте CRM), теряется только TG-уведомление; потеря тихая — админ не получает сигнала. | low | Осознанный компромисс модели «получатели по группе» (ADR-0022) + единый owner `crm-service` ([ADR-0039](./adr/ADR-0039-external-write-api.md) §Q-0039-1). Митигации при потребности: (а) CRM-side — предупреждение в UI при создании ящика без команды («Ящик без команды не получает Telegram-уведомлений»); (б) aggregator-side — маршрутизировать уведомления по unassigned-ящикам всем super_admin с живым `telegram_links` (или спец-каналу), либо разрешить назначить получателя. Отдельный ADR. |
| **TD-048** | Нет фоновой уборки осиротевших пустых групп (external team create, ADR-0042) | [ADR-0042](./adr/ADR-0042-external-team-create-delete.md) §Consequences. `POST /api/external/teams` не идемпотентно; при проигрыше CAS-гонки CRM (парный CRM `ADR-043` §4) или read-timeout между созданием группы и записью привязки в CRM создаётся **пустая** группа (0 ящиков, 0 участников, leaderless), не привязанная ни к одной CRM-команде. CRM реклеймит её best-effort через `DELETE /api/external/teams/{id}`, но при сбое DELETE группа остаётся. Фоновой уборки (скан «группа без ящиков/участников/лидера, старше N») нет. Также `PATCH`(rename) для синхронизации имени группы с переименованием CRM-команды не реализован (имя косметическое в headless). | Очень низкий: пустая группа безвредна (0 ящиков ⇒ 0 видимости/доставки/уведомлений; `GET /api/external/teams` отдаёт лишнюю строку в дропдауне CRM, но CRM показывает имя своей команды). Редкое событие (гонка первых-добавлений/таймаут). | low | При накоплении — периодический cleanup-job: `DELETE groups WHERE leader_user_id IS NULL AND NOT EXISTS(mail_accounts WHERE group_id=g.id) AND NOT EXISTS(user_groups WHERE group_id=g.id) AND created_at < now()-interval` (та же guard-логика, что external `DELETE`). Парный CRM `TD-037`. |
| **TD-049** | Демонтаж снятых подсистем ([ADR-0043](./adr/ADR-0043-strip-to-connector-push-to-crm.md)) — объём и порядок drop'ов | ADR-0043 §4/§5. При переходе в connector снимаются теги/Telegram/webhooks/forwarding/groups/roles/users(human)/external-teams+tags/MinIO/Jinja-UI. Демонтаж выполняется **последним** (decommission), после подтверждённой end-to-end доставки в CRM; до него агрегатор функционален (откат бесплатный). Drop-миграции (`mail_accounts.group_id`, `groups`/`user_groups`/`tags*`/`telegram*`/`webhooks*`/`*forward*`/`attachments*`, MinIO-bucket) + удаление 646 вложений — необратимая точка (нужен бэкап). **Исполнимый план — [ADR-0044](./adr/ADR-0044-decommission-runbook.md)** (инвентаризация 19 таблиц с вердиктами, FK-безопасный порядок §4, бэкап §6, снос фронта §5). | Средний по объёму, низкий по риску при соблюдении порядка (сначала CRM принимает push и вебхуки, потом drop). | medium | Выполнять строго по [ADR-0044](./adr/ADR-0044-decommission-runbook.md) §4 (согласован с runbook CRM `ADR-044` §10 шаг decommission); снять `EXTERNAL_REPLY_ENABLED`/reply ADR-0035; после стабилизации — удалить `TD-041`/`TD-042`/`TD-043`/`TD-047`/`TD-039`/`TD-034`/`TD-035` (они относятся к снятым Telegram/forwarding/tags-подсистемам) как более неактуальные. |
| **TD-050** | `admin_audit` не переносится в CRM + INDEX/docs-sync агрегатора ([ADR-0043](./adr/ADR-0043-strip-to-connector-push-to-crm.md)) | **(а)** `admin_audit` (248 записей, 2026-05-14…2026-07-10) НЕ мигрируется в CRM by design — у CRM аудит лог-based (structlog), БД-таблицы-аналога нет. Журнал доступен в БД агрегатора read-only до decommission; **при decommission обязателен дамп в бэкап** (не терять молча). **(б)** Статус-колонка `docs/adr/INDEX.md` для 15 superseded-ADR (0017/0018/0019/0022/0023/0024/0027/0030/0031/0033/0034/0035/0037/0040/0042) ещё `accepted` (forward-ссылки в шапках ADR **проставлены**); пометка «narrowed by ADR-0043» у ADR-0039/0041 не проставлена. **(в)** Нормативные docs агрегатора (`03-data-model`/`04-api-contracts`/`05-modules`/`07-deployment`) ещё описывают снятые подсистемы (tags/telegram/groups/webhooks/forwarding/MinIO). | (а) осознанно (лог-based аудит CRM); (б)/(в) — синхронизация docs выполняется вместе с реализацией strip-фазы (`ADR-0043`), чтобы не описывать несуществующий код раньше времени. | medium | (а) при decommission — `pg_dump` таблицы `admin_audit` в архив (devops-runbook, [ADR-0044](./adr/ADR-0044-decommission-runbook.md) §6); (б) обновить статус-колонку INDEX + narrowed-пометки; (в) вычистить разделы снятых подсистем из нормативных docs агрегатора синхронно с реализацией шагов [ADR-0044](./adr/ADR-0044-decommission-runbook.md) §4. Выполняется architect'ом в S2/S8 (CRM `ADR-044` §12). |
| **TD-051** | Рудиментарные колонки `users` после редукции до `crm-service` ([ADR-0044](./adr/ADR-0044-decommission-runbook.md) §1) | После демонтажа таблица `users` остаётся технической с единственным рядом `crm-service` (нужна из-за `mail_accounts.user_id` NOT NULL FK CASCADE — дроп невозможен). Колонки логина/ролей (`password_hash`, `password_encrypted`, `role`, `password_reset_required`, `lockout_until`, `failed_login_attempts`, `last_login_at`) становятся неиспользуемыми (входа/ролей в headless нет). Колонка `group_id` дропается в §4 шаг 7; остальные оставлены как есть — дроп не обязателен. | Очень низкий: несколько мёртвых колонок на 1 ряд; функционально безвредны. | low | Необязательный cleanup-миграция — `ALTER TABLE users DROP COLUMN` для рудиментарных колонок логина/ролей, если понадобится минимизировать схему. Отдельная backend-задача; на connector не влияет. |
| ~~**TD-052**~~ | ~~Headless OAuth-consent re-onboarding недоступен после сноса UI~~ **→ РЕШЕНО [ADR-0045](./adr/ADR-0045-external-outlook-oauth-headless.md)** | **Закрыт [ADR-0045](./adr/ADR-0045-external-outlook-oauth-headless.md)** (парный CRM `ADR-045`): `OutlookOAuthService` **остаётся** и адаптируется под headless (owner=`crm-service`, без `group_id`), consent-flow восстановлен внешними роутами `POST /api/external/mailboxes/oauth/authorize {crm_state}` + `GET /api/external/mailboxes/oauth/callback` (в сохраняемом `external/router.py`); привязка ящик↔команда — server-to-server HMAC-уведомление CRM `POST /api/mail/oauth/ingest` (реюз `CRM_PUSH_SECRET`). `ADR-0044` §7/Phase A3/Phase G амендированы: сохранить `OUTLOOK_REDIRECT_URI` (обновить на `/api/external/mailboxes/oauth/callback`), `OUTLOOK_CLIENT_ID`/`_SECRET`/`OUTLOOK_TENANT`/`OUTLOOK_OAUTH_STATE_TTL_SECONDS`; добавить `CRM_OAUTH_INGEST_URL`. | Остаток: personal-Outlook only (корпоративный O365 — CRM `TD-049`); OAuth e2e не подтверждён (`TD-031`). | — | — |
| ~~**TD-053**~~ | ~~Предписания [ADR-0046](./adr/ADR-0046-mailbox-status-hook-points.md) не реализованы в коде~~ | **Закрыт — реализовано полностью (коммит `e7c7b52`).** Все восемь hook-точек статус-канала **H1–H6/H7a/H7b** в коде; инвариант «enqueue строго ПОСЛЕ COMMIT» (ADR-0046 §2) соблюдён во **всех** восьми. Сделано: **(1)** hook после COMMIT в `worker/app/sync_cycle.py::_record_failure` (`:724`) — H3; **(2)** `last_synced_at` убран из `MailAccountsRepo.mark_sync_failure` (`backend/app/repositories/mail_accounts.py:540-575`) — §1, единственный писатель поля теперь `mark_sync_success` (`:480`); **(3)** `mark_oauth_needs_consent` пишет `last_sync_error = OAUTH_NEEDS_CONSENT_SYNC_ERROR` + hook (`backend/app/oauth/service.py:657-675`) — H7a; **(4)** guarded-`UPDATE` маркера в clean-skip-ветке воркера + hook **только при фактической записи** (`sync_cycle.py:624` → `:728-741`, repo `mark_oauth_needs_consent_error` `:411-439`) — H7b, идемпотентен; **(5) сверх плана:** H5/H6 приведены к §2 через **deferred-flush** (ADR-0046 §2.1 — сервис копит id, роутер флашит вне `db.begin()`: `accounts/router.py:319`, `external/router.py:511`); раньше пушили из открытой транзакции → в CRM мог уехать до-статус, а для деактивации это невосстановимо. Три копии hook-хелпера схлопнуты в единый `enqueue_crm_status_best_effort` (`backend/app/crm_push/service.py:187`). Тесты: кейс на каждую точку H1–H7b и каждое требование N1–N7 (`tests/worker/test_crm_status_hooks_adr0046.py`, `tests/worker/test_crm_status_hooks_api_adr0046.py`); CI-scope зелёный (1078 passed, coverage 56.41% при гейте 30%). Миграций/изменений контракта не потребовалось; backfill не понадобился (H7b самовосстанавливает за один цикл). | — | — | — |
| **TD-054** | Deferred-flush статус-канала (ADR-0046 §2.1) не имеет рантайм-детектора: забытый у нового вызывающего `flush_crm_status_events()` теряет статус-событие **молча** | [ADR-0046](./adr/ADR-0046-mailbox-status-hook-points.md) §2.1/§2.1.1/§5 п.4. Deferred-flush перенёс ответственность за отправку с сервиса на **вызывающего** — владельца транзакции (сегодня это роутер; норма §2.1.1 в том же объёме адресована фоновому job/CLI/обёртке): `MailAccountService` копит id в `_pending_status_account_ids` (`backend/app/accounts/service.py:111`; append `:725` — H5, `:766` — H6), а отправляет только `flush_crm_status_events()` (`:115-126`), вызываемая после COMMIT вне `async with db.begin():` (`accounts/router.py:319`, `external/router.py:511`). Новый вызывающий статус-пишущих методов (`update` при `creds_changed`, `set_active` — закрытый перечень ADR-0046 §2.1.1), **не** позвавший flush, не ловится **ничем**: список тихо уезжает в GC вместе с сервисом — ни исключения, ни лога (`flush_crm_status_events` просто дренирует список, `:123-126`); греп по `enqueue_crm_status_best_effort` (§5 п.3) остаётся **зелёным**, т.к. единственный HTTP-вызов хелпера живёт внутри самого flush (`:126`). Событие теряется навсегда; для **деактивации** это невосстановимо — ящик выпадает из `list_active()` (`repositories/mail_accounts.py:192-198`), второго статус-события не будет никогда → в CRM он навсегда «активный и зелёный». **Сегодняшний код корректен** (оба статус-пишущих роутера флашат) — это отсутствие защиты от будущей регрессии, а не действующий баг. | Низкая вероятность (2 статус-пишущих метода, перечень закрыт, греп §5 п.4 ловит на ревью) × невосстановимое последствие при деактивации (расхождение статуса CRM навсегда, без единого сигнала). Класс — «тихая потеря», как TD-045. | medium | **backend**: добавить **детектор** (не авто-flush — авто-flush отклонён в ADR-0046 §Alternatives как скрытая магия, привязывающая доменное событие к жизненному циклу сессии и способная отправить событие по откатившейся транзакции): при закрытии запроса/сессии, если `_pending_status_account_ids` **непуст** — `log.warning("crm_status_pending_dropped", mail_account_ids=…)` (прод) + жёсткий `assert`/fail в тестовом окружении. Варианты места: FastAPI-зависимость, конструирующая `MailAccountService`, либо async-context-manager сервиса. Требование: детектор **не отправляет** событие, а делает молчаливую потерю шумной; норма поведения роутеров при этом не меняется (ADR-0046 §2.1.1 остаётся в силе). Отдельного ADR не требуется — амендмент §2.1 уже описывает конструкцию. |

## Закрытые / отозванные пункты

| ID | Причина |
| --- | --- |
| ~~TD-001~~ | Закрыт. Смена пароля супер-админа выполняется через `.env` + restart `api`/`worker` (см. `07-deployment.md` sec. 11.1); `seed_super_admin` upsert'ит `users.password_hash`. UI для одного супер-админа сознательно не предусмотрен. |
| ~~TD-002~~ | Закрыт. Использован non-root service account: `MINIO_APP_*` создаются init-контейнером `minio-bootstrap` с политикой только на bucket `mail-attachments`. См. `07-deployment.md` sec. 12, `06-security.md` sec. 12. |
| ~~TD-009~~ | Отозван — это design-choice, а не tech-debt. `sent_messages` сознательно не подпадают под 30-дневную ретенцию (ADR-0011). |
| ~~TD-013~~ | Закрыт ADR-0022. Реализован полный flow Telegram push-уведомлений: линковка `telegram_links` + диспатчер `worker.tg_notify_dispatch` + opt-out `users_settings.tg_notifications_enabled` + дедуп `telegram_notifications`. |
| ~~TD-014~~ | Закрыт в рамках ADR-0022 migration plan: `shared/config.py` `BOT_TOKEN` → `TELEGRAM_BOT_TOKEN` (alias на переходный период), `.env.example` синхронизирован, devops применяет на prod. |
| ~~TD-021~~ | Закрыт round-27. Формализован переход `*_contains` substring→whole-word case-sensitive в ADR-0017 §4/§5/§7. |
| ~~TD-023~~ | Закрыт round-27. `\y`-границы (ломали паттерны с пунктуацией) заменены на граничные классы; ADR-0017 §4.1. |
| ~~TD-053~~ | Закрыт (коммит `e7c7b52`). [ADR-0046](./adr/ADR-0046-mailbox-status-hook-points.md) реализован полностью: все восемь hook-точек статус-канала (H1–H6/H7a/H7b) + инвариант «enqueue после COMMIT» во всех восьми (в HTTP-слое — deferred-flush, ADR-0046 §2.1) + `last_synced_at` = только успешный синк (§1) + единый хелпер `enqueue_crm_status_best_effort`. Тесты — по кейсу на точку H1–H7b и требование N1–N7. |

Регистр обновляется при каждом ADR/тех-решении, которое сознательно откладывает работу. Закрытые items не удаляются физически, а помещаются в раздел "Закрытые / отозванные".
